# 军力榜补数问题 —— 方案 v5（实施规格）

> v1 局部裁剪 → 撤回｜v2 观测口径不成立 → 重写｜v3 主线被接受｜v4 补齐定义
> **v5 相对 v4 的变更集中在第 0 节**，其余章节为完整规格（自包含，不必回看旧版）。

---

## 0. 相对 v4 的变更

| # | 变更 | 来源 |
|---|---|---|
| 0.1 | **`verdict_trusted` 改成「非 `None` 个数」**，正值另设 `verdict_positive` | 我核出的 0 分边界 |
| 0.2 | 明确 **`success_verdict` 单调**（只会减少重置），1a 的风险收敛到一个方向 | review + 0.1 |
| 0.3 | Phase 0 **第一步是行为中性重构**：抽出逐屏逻辑供首屏复用 | review |
| 0.4 | 诊断开关**走环境变量通路**（命令行方案在现有代码里不可行） | review，已核实 |
| 0.5 | **回放工具列为交付物** | review |
| 0.6 | 语料的初始状态改为**落库前逐坐标就地记录**，取消整体快照 | review |
| 0.7 | 估算配对**剔除跨周一、剔除期间有攻击记录**的样本 | review |
| 0.8 | 1b 补两条：修复写入**不计入 `written`**、名次只在原为空时补 | review |
| 0.9 | 离页那一屏必须补一条带结束原因的记录 | review，已核实 |
| 0.10 | 代码版本降级要求：至少记判据版本常量与包版本 | review |
| 0.11 | 5.1 删掉「重置总数下降」这条通过线（同语料回放下近乎必然） | review |
| 0.12 | 首屏进阀**拆两步**，Phase 0 先加 `screen_seq > 0` 闸保持现行为 | review |

### 0.1 为什么要改 `verdict_trusted` 的定义

v4 定义成「`Judgement.trusted` 里的**正值**个数」。但判据**允许采信 0**
（生产上名次 821 每趟读出 `0.0`）。于是：

```
trusted = [0, None, 0]  →  filled[1] = 0  →  目标带值（非 None）
  success_old      = 真      （它只问 military_score is not None）
  success_verdict  = 假      （正值个数为 0）
```

**单调性在这里破。** 改成「非 `None` 个数」之后不再破，见 0.2。
正值计数仍有诊断价值，另存为 `verdict_positive`。

### 0.2 `success_verdict` 是单调的

`filled = interpolate_scores(trusted)`，而插值要求**两侧邻居都可信**。所以：

```
fresh 里有带值目标  ⟹  它自己被采信，或它两侧被采信  ⟹  trusted 非空
即  success_old ⟹ success_verdict
```

**换条件只会减少重置，不会新增。** 所以 Phase 1a 的风险只剩一个方向 ——
**响得不够**，由 5.1 的独立连续失败计数兜底。

（v3 里「新条件不保证只响得更少」那句针对的是 v2 那个**带 bot 过滤**的候选，
对本版首选候选不成立。）

---

## 1. Phase 0 —— 只观测，不改行为

### 1.0 第一步：行为中性重构

首屏现在**完全走在滚动循环之外**：不打「采集一屏」日志、没有 `dry`、
没有重叠判断、**不进阀的计数**。而且 `for extra in range(1, 0 if reached_limit else ...)`
在首屏就采够时一次都不跑 —— 那一趟一条逐屏日志都没有。

所以 Phase 0 的第一步是把逐屏那段抽出来给首屏复用，**行为逐字不变**：

- 首屏记为 `screen_seq=0`，走同一套日志与影子计数
- ⚠️ **阀的实际更新加一道 `screen_seq > 0` 的闸**，保持现行为（见 0.12 / 6.1）

### 1.1 每屏计数（六个来源分开）

| 字段 | 取自 | 区分什么 |
|---|---|---|
| `verdict_trusted` | `Judgement.trusted` 里**非 `None`** 的个数 | 判据本身成没成功（**用于影子条件**） |
| `verdict_positive` | 其中**正值**个数 | 诊断用；0 分行的存在会让它与上一格不等 |
| `history_appended` | 本屏实际追加进历史的记录数 | 名次缺失会让采信值进不了历史 |
| `history_new_ranks` | 其中**不同名次**的个数 | 历史不按名次去重，四条记录未必是四个独立支撑点 |
| `bot_measured` | 过滤后 bot 行里的实读值个数 | bot / 坐标过滤吃掉了多少 |
| `fresh_valued` | 去重后新增带值目标数（阀**现在**看的那个数） | 重复观测吃掉了多少 |

⚠️ 必须由**同一次判定**交出来。`targets_from_rows` 把它已经算出的统计带出来，
**不是重判**。理由：历史追加发生在 bot / 坐标过滤**之前**。

### 1.2 影子评估 —— 记「成功」，不是「触发」

三个**成功**条件，各自维护独立影子失败计数，**连续两次失败记一次假想重置后归零**：

| 名称 | 条件 |
|---|---|
| `success_old` | `any(t.military_score is not None for t in fresh)`（现行） |
| `success_verdict` | `verdict_trusted > 0` |
| `success_history` | `history_appended > 0` |

现行阀照旧生效；三个候选都只影子运行。

⚠️ **产出的定性**：这只是「三种条件在**现行运行轨迹**上的比较」。换条件后历史会不同、
后续采信也会不同，**不能把影子事件当成换条件后的真实重置集合** —— 那个只有 1.5 的回放能给。

### 1.3 首选候选：`success_verdict`

反例：连续两屏分数正常但**名次全部缺失** → `verdict_trusted > 0` 而 `history_appended = 0`。
按 `history` 条件会清掉此前健康的历史，**却修不了名次缺失**；
而重复名次又能持续追加，`history_appended > 0` 也不代表曲线在有效推进。

**保证范围**：这道阀保护「**连续两屏没有任何值被采信**」这一类故障。
**不保证**解决局部污染，**不保证**解决历史停滞。

### 1.4 每次重置的上下文

阀基于连续两屏，只记触发当屏不够。每次重置记：屏序、重置前历史点数、
当时 `unread` 规模、**连续那两屏各自的 1.1 六个计数**。

每趟收尾另记：**最后一次重置在第几屏、收尾时历史多少点、补数三个数**。

### 1.5 逐屏原始输入采样

**开关的传递通路**（v4 的命令行方案不可行）：

```
2835:  command = ranking_command(...)               ← 命令在这里就建好了
2879:  run_id = uuid4()                             ← run_id 之后才生成
2880:  with child_environment(run_id=run_id, ...)   ← 运行器靠环境变量认领
```

所以**走 `child_environment` 那条环境变量通路**（或把命令构建挪到 run_id 之后，二选一）。

- 由**调度器**在起子进程那一步读取并清除该请求，与 `begin_mission_run` **同一事务** ——
  子进程崩了也不留残值
- ⚠️ 不许把布尔值长期写进可复用的任务参数
- ⚠️ **手工直跑的路径也要能开**（本机是备份挂机机）。
  注意 `child_environment` 的 docstring 已经记着：残值会让下一次手工直跑认领上一轮的 run_id

**内容**：开启时在「采集一屏」的 payload 里附带本屏**原始行数组**（名次、坐标、分数、空值，保留行序）。

**初始状态就地记录**（取代整体快照）：
落库那一步本来就逐坐标查过一次库，**成本不变**。在每屏落库**之前**，
把本屏**首次出现**的坐标当时的库内状态记进同一条日志：有没有行、军力值、估算标记、拉黑时刻。

> 为什么不用整体快照：拉黑判断走的是**按坐标查单行** ——
> 一个坐标没被本趟观测到，它拉不拉黑对回放没有任何影响。
> 就地记录还让语料自带初始状态，回放能验到「已有值 / 不可更新」那几档结果。

**完整性检查**（缺任何一项 → 该批语料标为不完整，不得用于验收）：

1. **首屏记为 `screen_seq=0`**；其后屏序连续、不重复；**空屏也必须记录**
2. **离页那一屏**（现在是 `break` 在算日志之前）必须补一条带**结束原因**的记录，否则屏序会断
3. 原始行数组长度与 `rows_read` 一致，行序与空值原样保留
4. 同一 `run_id` 下有开始记录、生效配置、**版本标识**、结束记录；
   结束记录说明：正常完成 / 达到上限 / 取消 / 异常 / 离页，以及**是否执行了收尾补数**
5. 收尾记录的总屏数、总行数与实际收到的语料一致；**缺结束记录的批次标为不完整**
6. **先用原版本回放**，复现采信统计、重置位置、收尾结果，再比较候选版本

⚠️ **版本标识**：运行机上不一定拿得到 git sha，**至少记 `SCORE_RULE_VERSION` 与包版本**。

**采样覆盖**：首批 4–5 趟，必须覆盖 —— 正常趟、含重置的趟、含重复观测修复机会的趟。
未覆盖的情形用**构造用例**补，**不得声称「生产已验证」**。

### 1.6 Phase 0 的交付物

1. 三种成功条件在现行轨迹上的差异（含「哪些屏会翻向」）
2. 最后一次重置的位置分布，及其与收尾历史规模 / 补数率的关系
3. 重复观测的可修复规模（按**不同坐标**计数）
4. 一批通过完整性检查的可回放语料
5. **回放工具本身** —— 能从录下的行数组驱动判定 / 去重 / 阀 / 收尾补数，并接假仓储
   （v4 把它当成免费的，它不是）

**不设通过线。**

---

## 2. Phase 1a —— 换阀的成功条件

依 1.2 的影子数据 + 1.5 的语料**配对回放**后选定，首选 `success_verdict`（单调，见 0.2）。

⚠️ 两屏阈值保持不动，不许同时改两个变量。不许靠调高阈值掩盖口径问题。

---

## 3. Phase 1b —— 重复观测补回目标

### 3.1 状态规则（本趟内，按坐标）

| 观测序列 | 应有结果 |
|---|---|
| 空 → 空 → 实读 A | 补入 A |
| 空 → 估算 → 实读 A | 估算不更新；随后补入 A |
| 空 → 实读 A → 实读 B → 空 | 保留 A；后续不覆盖、不重新进入 `unread` |
| 首次已有实读或估算 | 保持原值（本次只补空） |

⚠️ 第三行的最后一步**现有去重已经保证**（`unread` 只从 `picked` 来，
而已见坐标进不了 `picked`）——**不需要额外代码，但要有用例锁住**。

### 3.2 约束

1. **潜在收益按不同坐标计数。** 「空→实读A→实读B」只算**一个**可修复目标；
   Phase 0 需独立的模拟状态，不能每次拿首次空值去比。
2. **修复更新不许混进 `fresh`。** 否则会改变新增计数、连续空屏数和停扫行为。
3. **达到 `bot_limit` 后**：可处理**本屏已读到的**修复机会，不新增目标、不额外滚屏。
4. **修复写入不许计入 `written`。** `written` 现在来自 `backfill_missing_military_scores`，
   收尾那句「真正补进库 N 条」说的是**曲线补数**；混进来会让它虚高。
5. **名次照现有补数的做法**：只在原来为空时补，不覆盖。

### 3.3 新的仓储契约

现有两个都不能用：`backfill_missing_military_scores()` 写死 `military_score_estimated = True`；
`save_ranking_targets()` 无条件覆盖。

> **只更新现存、未拉黑、且更新时仍为空的目标；不创建新行；
> 写入实测值（非估算）、用本次观测时间；返回逐坐标处理结果。**

「未拉黑」继承自 `save_ranking_targets` 现有保护
（`if target.blacklisted_at_utc is not None: continue`）。

| 逐坐标结果 | `unread` 的处置 |
|---|---|
| 写入成功 | 移出 |
| 已有值 | 移出 |
| 不可更新（不存在 / 已拉黑） | 移出，且**不得再进入曲线补数** |
| 失败 | **保留**，不得当作成功 |

---

## 4. Phase 2 —— 锚点改取自同一次判定

`next_score_anchor` 不再走第二遍无曲线判定，改取本屏 `Judgement.trusted` 的最大正值。
单独提交、单独验证。

⚠️ **不得要求它减少空值** —— 它修的是锚点来源，验的应是锚点来源正确 + 行为改善。

---

## 5. 验收条件

**基线口径**：所有「不降 / 改善」都必须与**旧版本在同一份语料、同一共同集上**的结果比。
⚠️ 不得拿历史总体数（如 73%）当共同集基线。

### 5.1 Phase 1a

| 项 | 通过线 |
|---|---|
| 连续失败保护 | 独立计数（不用会被重置归零的那个）；**用构造序列验证能恢复** |
| 最后一次重置位置 | 后移，且收尾历史规模上升 |
| 补数 | 分三个数报：曲线推得出 / 真正写入 / 最终仍为空 |

⚠️ **删掉了 v4 的「重置总数下降」** —— 同语料回放下它近乎必然成立
（`success_old ⟹ success_verdict`），写成通过线没有鉴别力。
真正的考题是：构造一段**被毒历史里每屏漏进一两个采信值**的序列，
验独立计数能不能把它报出来。（这类故障已由 1.3 划到保证范围之外，所以只验「能报出来」。）

### 5.2 Phase 1b

⚠️ **不要求「最终缺值必须下降」** —— 旧流程估算 9500、新流程实读补 9800 时缺值都是 0，
但 1b 确实做了有效修复。

| 项 | 通过线 |
|---|---|
| 最终缺值 | **不增加** |
| 实读修复数 | 有合格机会时**必须增加**；**单列**，不并入曲线补数率 |
| 状态表符合性 | 数值、估算标记、时间戳、不覆盖行为逐条符合 3.1 |
| 仓储结果 | 四种结果与 `unread` 的处置逐条符合 3.3 |
| `written` | 不含修复写入 |

### 5.3 估算质量（1a / 1b / 2 通用）

| 项 | 通过线 |
|---|---|
| 共同可补集准确率 | 相对**同一共同集上的旧版结果**下降 ≤ 2 个百分点 |
| 新增可补集准确率 | ≥ 共同集旧版结果 − 5 个百分点 |
| 严重高估 | 定义：估算值 > 真值 × 1.5。新增集里占比 ≤ 2%，且绝对条数不增加 |
| 误差尾部 | P99 用绝对相对误差；**另报高估方向的尾部**；样本量与无法配对比例同报 |
| 配对方式 | 按**坐标**配对，报配对成功率与时间间隔 |
| **配对排除** | **剔除跨周一 UTC+0 的配对**（bot 军力每周一刷新）、**剔除两次观测之间有攻击记录的配对**（打成平局会削弱军力）；**报剔除比例** |

### 5.4 选靶影响

不只报「前列变了没有」—— **新进入前列的目标必须逐个检查有没有被严重高估**
（前列变化本身可能正是改进）。

### 5.5 诊断指标（不设上下界）

丢弃率、各理由占比、曲线覆盖行数、`verdict_positive` 与 `verdict_trusted` 的差。

---

## 6. 排序与不做

### 6.1 首屏进阀 —— 拆两步

- **Phase 0 的重构**把首屏纳入逐屏日志与影子计数，
  但阀的实际更新加 `screen_seq > 0` 的闸，**保持现行为**
- **1a 落地并评估后**，单独一个小提交把闸拿掉

风险很低：首屏时锚点为空、历史为空，它失败只可能是 OCR 整屏读废；
前两屏都失败时清掉的历史最多只有一屏的点。收益是堵上第 1.0 节那个覆盖漏洞。

### 6.2 明确不做

- 局部裁剪（v1）—— 直到 Phase 0/1 数据出来
- `screen_consensus` —— 已撤回（独立验证 58%，n=19）
- **判定与补数分离状态** —— 暂留共用。若修完误触发后**真实重置**仍主导历史损失，再评估
- 不同时调两屏阈值

---

## 7. 仍不确定

1. **1.5 的「同一事务」具体指什么？** `begin_mission_run` 在 `child_environment` 之后
   （2882 行），而开关需要在**起子进程之前**读到。读取与清除若分处两个时机，
   崩溃窗口仍然存在，我没想清楚怎么收口。
2. **回放工具的假仓储要模拟到什么程度？** 1.5 的就地记录解决了初始状态，
   但收尾补数还会写库、`unread` 的移出依赖写入结果 ——
   假仓储要不要模拟写入失败这一档？不模拟就验不到 3.3 的「失败必须保留」。
3. **`verdict_positive` 与 `verdict_trusted` 长期不等意味着什么？**
   0 分行是榜上真实存在的（名次 821 每趟读出 `0.0`，15 趟里被丢 11 次）。
   它反复被判据丢掉是不是本身就是个该修的小问题？不在本方案范围内，但值得记一笔。
