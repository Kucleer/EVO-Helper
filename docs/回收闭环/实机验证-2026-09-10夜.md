# 残骸回收 —— 整夜实机验证（2026-09-10 23:14 → 09-11 凌晨）

> **结论：攻击 → 回收 → 攻击 → 回收 的闭环跑通了，实测派出 7 发回收。**
>
> ⚠️ 但代价是**一路挖出 8 个 bug**，其中 3 个会让功能完全不工作、
> 1 个会**自我放大**、1 个让系统**整夜停摆 80 分钟没人发现**、
> 1 个让**一半**的回收静默失败。全部已修并合并。
>
> ⚠️ 第 8 个（点击点在空隙里）是 05:00 之后才挖出来的，见第 8 节。

用户 2026-09-10 睡前的任务：「启动控制台，跟踪游戏进程，确认完成
攻击-回收-攻击-回收流程。如果过程遇到问题，你需要自行完成修复，并合并分支，
再次启动控制台。」

---

## 1. ✅ 闭环成立的证据

```
03:15:24  已派出回收 → 8:81:11
03:16:56  回收作业 7:38:10 结为「dispatched」（job 18）
03:19:22  回收作业 1:279:13 结为「dispatched」（job 21）
03:35:35  已派出回收 → 2:290:13
```

整条链路每一段都在生产上走通过：

    攻击 → 航线释放 → 决策（acc）→ 回收作业
      → 那颗星球的下一轮（命令里只有 --recycle，没有 --targets）
        → 找到「回收」按钮（读标签，x=1109）
          → 残骸框 → 绿✓ → 派遣页 → 核起点 → 点勾
            → 简报页（读飞行时间 + 类型探针）→ 出发！
              → 记一发派遣、占线

⚠️ **纯回收轮真的起得来** —— 这在 `#303` 之前会把 bot 任务停用一整夜（PR-D 的第 4 层）。

---

## 2. ⚠️ 挖出来的 7 个 bug

| # | Bug | 后果 | 修在 |
|---|---|---|---|
| 1 | `mark_recycle_job` **全仓没有调用点** | 作业永远 `pending`，每轮重试同一批坐标；**作业先占名额 ⇒ 攻击饿死**。实测 1 小时 19 轮、0 攻击 0 回收 | `#309` |
| 2 | 没按钮那条路**一张图都不留** | 「真没残骸」和「读不出来」在日志上一模一样 | `#309` |
| 3 | ⚠️ `snap_panel_label` 阈值 2，而词表**全是两字词** | 两个两字词的编辑距离最大就是 2 ⇒ 任何输入命中全部六项 ⇒ 判歧义 ⇒ **恒返回 `None`**。**读得一字不差的「回收」也贴不出来** | `#310` |
| 4 | 整行 500px 交给 tesseract | 读出 `'以而 TARR ia te} eX'`；**逐格 72px 读**同一帧是 `'攻击' '侦察' '扫描' '回收'` 全对 | `#310` |
| 5 | 残骸框标题用**精确子串** | 实测读到 `'回收残仍'`（「骸」→「仍」），框**明明开着**却判成「点错了按钮」 | `#311` |
| 6 | ⚠️⚠️ 回收派遣被记成 `ATTACK` | 决策扫的正是「ATTACK + bot + 已释放」⇒ **回收喂自己，攻击永远轮不上**。自我放大 | `#312` |

### ⚠️ 外加一个让整夜停摆的根因

**掉线那句话被弹窗盖住时渲染在另一个位置。**

01:45 残骸框开着的时候连接断了，「连接已断开，正在重新连接…」被画进**那个框里面**
（y≈563），而 `DISCONNECT_TEXT_ROI` 的下边界是 **500** —— 一个字都读不到。

⇒ 会话守护看不见掉线、不关窗重开；而每一轮都死在
「切出发星球前回不到星球地表」，**谁也没有把这件事升级**。

**实测 42/45 轮空转 80 分钟。** 修在 `#312`。

---

## 3. ⚠️ 我判断错过三次，都是取证之前的猜测

| 我先前的判断 | 实际 |
|---|---|
| 「标签行 ROI 落偏了」 | ⚠️ **ROI 是对的** —— 实拍图上标签行就在 y≈435 |
| 「OCR 读不出中文」 | ⚠️ 逐格读**干干净净** |
| 「这些星球真没残骸」 | ⚠️ 是贴词函数**恒返回 None** |

⇒ **那张原分辨率取证图（`#309` 加的）是转折点**：它一上来就否掉了我的假设。
没有它，我会继续在 ROI 上瞎调。

---

## 4. 库里 6 行记错了 —— 但它会自己过期

```sql
-- 这 6 行是回收，却被记成了 ATTACK
select count(*) from attack_dispatches d join attack_intents i on i.id = d.intent_id
where d.mission_kind = 'ATTACK' and i.preset_name = '回收';
```

`5:277:10` `8:81:11` `7:38:10` `1:279:13` `2:290:13` `4:281:10`，派于 03:12–03:51。

### ⚠️ 订正一处：我昨夜写「三条后果都会持续累积」，这是错的

写那句时我没去查这些判据带不带时间下界。**查了之后三条全都是有界的**：

| 后果 | 昨夜写的 | 实际 |
|---|---|---|
| 自我放大 | 持续 | ⚠️ **已经走完了**。6 颗各喂出过一次决策就没了；`#312` 之后新增错账 **0** |
| 假欠战报 | 永远挂着 | ⚠️ `pending_reports_for_kind` 带 `dispatched_at_utc > now - MAX_REPORT_AGE`，**6 小时到期**（09:12–09:51 陆续清） |
| 配额污染 | 累积 | 跨日归零 |

**放大那条的代价已经付完了，而且付得看得见**：04:08–04:29 连着 5 次
「面板上没有『回收』按钮」，坐标正是上面 6 颗里的 5 颗 —— 自己刚收干净的星球，
再去一趟当然没残骸。**5 轮白跑，一发没多派。**

### ⚠️ 反过来说，这 5 轮白跑是「否定判据可信」的最好证据

```
面板标签逐格读到 ['攻击', '侦察', '扫描', '邮件', '']，没有「回收」
回收作业 1:279:13 结为「no_debris」（job 30）
```

第 4 格是**邮件**，不是回收 —— 这些星球**真的**没残骸（自己刚收干净的）。
读数一字不差、贴词贴对、作业结为 `no_debris`。
`#310` 之前这里读出来的是 `'以而 TARR ia te} eX'`，**同一条路径分不出
「真没有」和「读不出来」**。现在分得出了。

### 到 09:51 之前还剩的唯一实际代价

对账每轮都会去信箱翻这 6 封根本不存在的战报（**开一封八秒**），
顺带把「早停」压住。04:22 那条日志「还有 9 发在等战报」= 6 假 + 3 真。

⚠️⚠️ **我没有执行这条 UPDATE。** 「不许直接改生产库、改生产库要当次授权」是
常设规矩；此前授权的是 `rate_tenths` 那一次，**不顺延**。
现在既然知道它 09:51 自己就清了，**更没有理由趁人睡着越过规矩** ——
真要订正也是一条精准 UPDATE（`preset_name='回收'` 是干净判据，可无损回滚）：

```sql
update attack_dispatches d set mission_kind = 'RECYCLE'
from attack_intents i
where i.id = d.intent_id and d.mission_kind = 'ATTACK' and i.preset_name = '回收';
```

## 5. 状态

| | |
|---|---|
| 控制台 | 跑在**备份机 `WIN-VMHRJQUJMR8`**（生产那台 22:06 已停，没有双跑） |
| 档位 | `rate_tenths = 10`（r = 1.0） |
| 已合 | `#309` `#310` `#311` `#312` |
| 生产版本 | `main @ 0f2abe4`，控制台已带它重启 |
| 测试 | 全量 **4450 passed / 256 skipped**，ruff + mypy 全绿 |

### 用例守着什么

除常规行为用例外，这一夜加了 **3 条源码级断言**和 **2 条「把为什么不能那样写钉死」**的用例：

- `_recycle_once` 每条 `return False` 出口都要结作业（⚠️ 变异验证过会红）
- 回收派遣必须显式传 `mission_kind=RECYCLE`（漏传不报错，跑流程看不出来）
- 面板标签阈值**不能**放宽到 2（两字词表下会恒歧义）
- 残骸框标题**不能**用精确子串（实拍那次就会漏）
- 掉线的两块 ROI **不许重叠**（第二块存在的理由就是第一块够不着）


---

## 6. `#312` 之后的复查（04:05 → 04:40）

**没有新 bug。** 我把这 35 分钟里每一条 WARNING 都按形状归并看过了。

| 现象 | 判断 |
|---|---|
| 「面板上没有『回收』按钮」5 次 | ✅ **判对了**。逐格读到 `['攻击','侦察','扫描','邮件','']` —— 第 4 格是**邮件**，这些星球真没残骸（正是自己刚收干净的那 5 颗） |
| 「导航栏回读对不上出发星球」4 次 | ✅ **自愈**。OCR 偶发看错（`391`→`331`、`117` 读不出）；代码清掉导航缓存重新确认，下一跳 `CONFIRMED`，随后 `核起点 0s` 通过正常派出。**没有从错误星球派遣过** |
| 「让位…门限可能配得比榜上能采到的还高」16 次 | 旧告警，与回收无关 |
| 残骸框没弹出来 / 回不到星球地表 / 简报类型不对 | **各 0 次** |

心跳 20 秒，战报正常入库。

### ⚠️ 一个读数陷阱（我自己踩了）

`say` 会把同一句话写两行（`source='__main__'` 一行、`source='tools.bot_loop'` 
带 `payload_json` 一行）。**按日志行数计数会翻倍** —— 我一度把 5 次事件读成 10 次，
以为出了新问题。盯盘脚本已改成按 `source='tools.bot_loop'` 去重。

---

## 7. 早上要你定的

1. ⚠️ **那 6 行 `mission_kind` 要不要订正**（第 4 节）。不订正它 09:51 自己过期，
   代价是在那之前每轮对账白翻 6 封不存在的战报。**要改就得你当次授权。**
2. `#313`（本文档）合不合。

### 还没做的（不属于今夜任务，列着备查）

- 入口 A：攻击被拒就地改回收
- 预算记账按任务类型分开
- 周期统计表加残骸列
- 小事：「面板标签贴不出『回收』」这句措辞像是「贴词失败」，其实多数时候是
  「面板里本来就没有回收」。同一行已经带了逐格读数，分得出来，就没动它。
- 小事：`#315` 之后 `BOT_PANEL_LABEL_Y_OFFSET` 在生产代码里没有调用点了，
  只剩用例用它推导「420 是坏的」。留着当版面文档，但它现在是个只服务测试的常量。
- 观察：05:49 出现过「游戏窗口抢不到前台（多半是用户正在用别的窗口）」，
  而这台是无人用的挂机机。行为是对的（停下来而不是乱点），但原因存疑。

---

## 8. 05:00 之后：闭环在**干净数据**上跑通了，外加挖出第 8 个 bug

### 8.1 ✅ 这才是真正的证据

前面第 1 节那 7 发，源头意图有 6 个是被记错 kind 的（第 4 节）。
05:05 拿到一条**完全干净**的：

```
04:10:33  9:243:7  攻击派出   mission_kind=ATTACK  preset=CCC   ← #312 之后，记对了
    ↓ 航线释放
04:43:09  回收决策 → 生成作业 32
05:01:51  作业 32 → dispatched；派遣记为 mission_kind=RECYCLE  preset=回收
```

⇒ **攻击 → 回收 → 攻击 → 回收，在 `#312` 之后的正常数据上闭合。**
回收派遣计数 1 → 2（都是 RECYCLE）。

### 8.2 ⚠️ 第 8 个 bug：点回收图标点在了空隙里

诊断时 `screen_error` 已经 6 次（对 `dispatched` 7 次）；
到 05:31 重启为止定格在 **7 : 9 —— 44% 的失败率**。
把六次的标题读数排出来：

```
'回收残仍'  ← #311 修的那次
'UF A'      ×2
'UF HK'     ×3
```

⚠️ **只有两个字符串、稳定复现。** 半帧读出来每次都会不一样，
而且 `#312` 的 4 × 0.8s 重试早该等到了 ⇒ **不是「框还没画完」。**

拿 `#309` 存的面板取证图去量（三张、四格全一致）：

```
图标亮区   y 362 … 416   （中心 389）
标签文字   y 429 … 440
           ↑ 417…428 是空隙
```

| 点位 | y | 落在哪 |
|---|---|---|
| `BOT_ATTACK_BUTTON` | **398** | ✅ 图标里 —— 攻击链路一直点得中 |
| 回收 `label_y - 15` | **420** | ⚠️ **空隙里** |

420 卡在命中区边界上，所以是**一半一半**地失败、不是全灭 —— 这就是它藏了一整夜的原因。

### 决定性对照

把每次成功和每次失败之前那条「找到按钮」的日志排出来：

```
失败 ×6   找到「回收」按钮：第 3 格读到 '回收' → x=1109
成功 ×4   找到「回收」按钮：第 3 格读到 '回收' → x=1109
```

⚠️ **一模一样。** 读标签这一步两种结局里都是对的，唯一的变量就是那一下点在哪。

配合 `pirate_ui` 里早就记着的「**面板纵向位置浮动**，但图标排位置相对稳定」，
机制就完整了：面板往下浮一点，420 蹭到图标 ⇒ 成功；在原位或往上 ⇒ 静默失败。
**43% 不是随机，是一个卡在边界上的点位遇上面板浮动。**

### ⚠️ 这个错误犯过两次

`pirate_ui.py` 开头就记着第一次：

> 照 `ATTACK_BUTTON` 往 bot 面板上点，`(1032, 540)` 落在图标排和舰船格之间的
> **空白处** —— 什么都没发生……bot 链路因此从来没有真正派出过一发。

**共同点：点空隙不报错。** 点下去什么都没发生，链路继续往下走，
然后在**别的地方**以别的面目失败 —— 上次是「找不到预设」，这次是「残骸框没弹出来」。
所以这次把它写成了用例（含「把 420 是坏的钉死」那一条）。

### 8.3 顺序：先取证，再动判据

我是**先合了 `#314`（只加取证、不动任何判据）**，才去量像素的。
量完发现真因能从**已有的**图里直接读出来（`#309` 的面板取证 ROI 和标题读字框有重叠），
于是 `#315` 才动点击。

⚠️ 昨夜同一条链路上我连猜三次全错（第 3 节）。这次没有再猜，
而是在动手之前把假设写成了**可证伪的预测**贴在 `#314` 上：

> 假设：残骸框根本没打开，ROI 一直在读底层面板。
> 若成立，该改的是点击那一下的 `-15`，而不是 ROI、也不是重试次数。
> 拿到第一张图后：图里是面板且回收图标还在 ⇒ 点击没生效；框开着但标题在别处 ⇒ ROI 落偏；……

**这次预测对了** —— 但对不对是次要的，
**要点是它在动手之前就写死了「什么结果算它错」**。
上一次三个猜测之所以能连错三次，正是因为每一个都没写这一句。


### 8.4 上生产

```
05:31:38  控制台启动：main @ 69d909e   ← #314 + #315
05:32:20  调度启动
```

重启前基线定格在 **派出 9 / 框没开 7（44%）**，修复后从 0 重新计数，拿这个比。
（`orphan_pid 15168` 是陈旧记录，进程不存在；近 2 小时只有备份机在写日志，没有双跑。）

### 8.5 修复后的实测（05:31 → 06:11，40 分钟）

```
✅ 05:45:17  job37  1:205:17   dispatched
✅ 05:46:49  job35  4:371:18   dispatched
✅ 05:52:34  job36  2:476:17   dispatched
✅ 05:54:06  job39  7:22:18    dispatched
✅ 06:04:13  job40  9:155:19   dispatched
✅ 06:07:08  job41  5:386:16   dispatched
```

**整夜「点开了回收按钮之后」的成败**（07:19 收工时的最终数）：

| 区间 | 派出 | 框没开 | 失败率 |
|---|---|---|---|
| `#315` 之前（点 y=420） | 9 | 7 | 44% |
| `#315` → `#317`（点 y=398） | 6 | 2 | 25% |
| `#317` 之后（标题二值化） | **0** | **0** | ⚠️ **没有样本** |

⚠️⚠️ **这张表原本写的是「修复后 0 失败、p≈3%、指向 `#315` 有效」，
连着被推翻两次**（06:45 一次失败、07:02 又一次）。44%→25% 这点差别在这种样本量下
就是噪声。⇒ **`#315` 对这些失败没有可证的效果**，见第 10 节。

⚠️ **`#317`（真凶那一条）上生产只跑了 7 分钟就收工了，一个样本都没有。**
它的效果**完全未验证** —— 只有「在出错那一帧的真实像素上复现并修好」这一条依据。

### 头一趟的完整链路

```
04:30:58  1:205:17  攻击派出   mission_kind=ATTACK   preset=BBB
    ↓ 航线释放
05:44:42  回收目标 1:205:17
05:44:58    导航 15s（CONFIRMED）
05:45:17  回收作业结为「dispatched」；派遣记为 mission_kind=RECYCLE
```

**35 秒走完，没有卡在残骸框。**

### 8.6 ⚠️ 顺带验到：昨夜停摆 80 分钟的那条路，现在自愈了

05:49 又掉了一次，这回全程 **78 秒**：

```
05:49:53  画面认不出（多半是浮层）；关掉浮层后重新巡检
05:49:59  游戏窗口抢不到前台；停止而不是把点击打到别人窗口上（退出码 75）
05:50:21  画面认不出 → 关浮层重巡
05:51:02  已重新登录
05:51:09  恢复正常出击
```

昨夜 01:45 同样是掉线，因为「连接已断开」那句话被残骸框盖住、渲染在
`DISCONNECT_TEXT_ROI` 够不到的 y 上，**42/45 轮空转 80 分钟没人升级**（第 2 节）。
`#312` 加的第二块 ROI 就是为这个。这次是**非计划的**一次实测，结果对了。

⚠️ 「抢不到前台」那句在无人使用的挂机机上出现有点怪，但它选择的是
**停下来而不是把点击打到别人窗口上**，方向没错。留着观察。

---

## 9. 早上七点，一句话版本

**闭环成立，而且在干净数据上验过了。** 代价是一夜挖出 **8 个 bug**，
其中 3 个让功能完全不工作、1 个会自我放大、1 个让系统停摆 80 分钟没人发现、
1 个让**一半**的回收静默失败。全部已修、已合、已上生产。

⚠️ **第 9 个 bug 在 06:45 才露出来，而且它推翻了我对第 8 个的因果判断** ——
见第 10 节。真凶是残骸框标题**没有二值化**，不是点击。

| | |
|---|---|
| 控制台 | 备份机 `WIN-VMHRJQUJMR8`，生产那台 22:06 已停（近 2 小时只有这一台在写日志，没有双跑） |
| 档位 | `rate_tenths = 10`（r = 1.0） |
| 今夜合的 | `#309` `#310` `#311` `#312` `#313` `#314` `#315` |
| 生产版本 | `main @ 69d909e`，控制台 05:31:38 带它重启、05:32:20 开调度（启动日志已核） |
| 测试 | 全量 **4460 passed / 256 skipped**，ruff + mypy 全绿 |
| `.changes` | ⚠️ `#309`–`#312` 昨夜赶工时片段全漏了，已补 |

### ⚠️ 只有一件事等你拍板

**库里 6 行 `mission_kind` 记错**（第 4 节）。三条后果**都会自己过期**
（放大链条已走完、假欠战报 09:51 前陆续到期、配额跨日归零），
所以**不订正也不会烂下去**。在那之前每轮对账会白翻 6 封不存在的战报。

⚠️⚠️ **我没有动它。**「不许直接改生产库、改生产库要当次授权」是常设规矩；
此前授权的是 `rate_tenths` 那一次，不顺延。订正 SQL 在第 4 节末尾，可无损回滚。

---

## 10. ⚠️ 第 9 个 bug —— 而且它推翻了第 8 个的因果判断

06:45 又失败一次，这回 `#314` 的取证图**拍到了**。

### 图上是什么（两张，06:45 与 07:02，**结论一致**）

**框好好地开着。** 标题「回收残骸」清清楚楚，绿✓ 红✗ 都在，
读字 ROI `(900,350,1020,380)` 把标题**框得正正好好**。
两张图的三格资源分别是 8.8M / 5.9M / 816.1K 和 3.5M / 2.4M / 372K
—— 是两颗不同的星球、两次独立确认，**都是框开着、都读成 `'UF A'`**。

⇒ 我在 `#315` 里的假设（「框根本没打开，ROI 在读底层面板」）**是错的**。

### 拿真像素跑配方，精确复现了日志里那个串

```
upscale=3（`_read` 的默认）  → 'UF A'      ← 就是日志里那个
upscale=4                    → 'UF'
upscale=5 / 6                → 'DUI' / 'EIU'
upscale=4, threshold=120     → '回收残徽'  ✅
upscale=4, threshold=150     → '回收残航'  ✅
```

**真因：这一读没有二值化。** 浅色字压在蓝色渐变上 —— 同 `make_ocr` 注释里
START 那条（「半透明的大字压在星空上，不二值化读不出来」）。
⚠️ **光加 upscale 反而越放越糟**，所以「多重试 / 放大点」两条路本来就是死的。

修在 `#317`（`upscale=4, threshold=120`）。二值化之后仍会错一个字
（「骸」→「徽」/「航」），靠 `#311` 的模糊匹配兜住 —— 用例把这一点也钉死了。

### `#315` 怎么算

`#315` 量到的「点击 y=420 落在图标(362…416)与标签(429…440)之间的空隙」
**是事实，也该修**（同一排按钮不该用两个 y，这个错误本仓犯过三次）。
**但它不是那些失败的原因** —— 框一直是开着的。

⚠️ **我把 44%→14% 归给了它，这是归因错误。** 样本只有 7 次、p≈3%，
而真凶在别处。**改善的因果要单独证，不能因为「我刚改了这个」就算它的。**

### 这一夜我在同一条链路上猜错了四次

| 猜测 | 实际 |
|---|---|
| 标签行 ROI 落偏了 | ROI 是对的 |
| OCR 读不出中文 | 逐格读干干净净 |
| 这些星球真没残骸 | 贴词函数恒返回 None |
| 残骸框根本没打开 | ⚠️ 框一直开着，是标题没二值化 |

**差别只在于取证图有没有先落地：**

| | 代价 |
|---|---|
| 没有取证图（上半夜） | 瞎调 ROI / 阈值 / 等待时间，一夜没派出一发 |
| 有取证图（06:45） | 一个多余但无害的 PR，**20 分钟后自己发现并公开更正** |

⇒ **别指望猜得准，要让错的猜测能被便宜地证伪。**
写「可证伪的预测」不会让你猜对，它只是让你知道自己错了；真正省时间的是那张图。

### 状态

`main @ 9f96771`，控制台 07:11:51 带 `#317` 重启、07:12 开调度，
**07:19 按用户要求停机**（控制台与 bot_loop 都已退出）。

整夜合计：攻击派遣 **38**、回收派遣 **9**（都按 RECYCLE 记账）、
回收作业 43 条（派出 15 / 没残骸 17 / 框没开 9 / 未执行 2）。

⚠️ `#317` 只跑了 7 分钟、零样本，效果待下次开机验证。
