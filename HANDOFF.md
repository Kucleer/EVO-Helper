# EVO-Helper 交接文档

> 生成于 2026-08-08。下一个会话请先读本文件，再读 [SUBAGENT_IMPLEMENTATION.md](SUBAGENT_IMPLEMENTATION.md)。
> 本文件记录**环境前提、已验证事实、踩过的坑**——这些东西不写下来，下一轮会重新踩一遍。

## 1. 当前状态

- 仓库 `Kucleer/EVO-Helper`，`main` 在 `78150af`，**0 个未合并 PR**
- 494 个测试通过，`ruff` / `mypy` 全过
- **演习模式（dry run）是默认，至今一次真实派遣都没执行过**

已经能用的东西：

| 能力 | 状态 |
|---|---|
| 本地控制台（任务中心 / 情报中心 / 运行详情 / 诊断） | 可用 |
| 情报检索（坐标范围 + 舰队条件树 + 保存筛选器） | 可用 |
| 战报解析 → 入库 → 前端展示 | 可用，实测 17 行舰队全对 |
| 代码驱动截图（`capture_window`） | 已验证，含 WebGL canvas |
| 游戏窗口自动拉起（`ensure_game_window`） | 已验证，3.5s |
| 断线重连（10 分钟巡检） | 已验证 |
| 坐标扫描 | **已正式化**：`tools/scan_coordinates.py`，按优先级顺序扫、游标续扫 |

## 2. 环境前提（最容易踩的地方）

### 2.1 系统缩放是 125%，不是 100%

`CLAUDE.md` 里写的「系统缩放 100%」**与实机不符**。物理 1920×1080，逻辑 1536×864。

**任何要读窗口尺寸或点击的进程，第一件事必须是：**

```python
import ctypes
ctypes.windll.shcore.SetProcessDpiAwareness(2)  # PER_MONITOR_AWARE_V2
```

不声明的话拿到的是逻辑像素，窗口怎么调都对不上标定视口，而且**看起来没有任何报错**。

### 2.2 窗口几何

| 项 | 值 |
|---|---|
| 标定页面视口 | 1920 × 879（`layout_for_viewport` 只认这个尺寸） |
| app 模式标题栏 | 38 物理像素 |
| 窗口边框 | 宽 18、高 9 |
| **目标窗口尺寸** | **1938 × 926** |

`ensure_game_window()` 已经把这套换算封好了，直接用。

**最大化的窗口对 `SetWindowPos` 免疫**，必须先 `ShowWindow(SW_SHOWNORMAL)` 还原。

### 2.3 必须用 `--app` 模式启动游戏

```bash
chrome.exe --app=https://eternal-void.online/
```

普通 Chrome 窗口把标签栏、地址栏、书签栏画在 **client area 之内**，裁剪只能去掉阴影去不掉这些——
每张截图都会带上用户的标签页标题和书签，而且页面视口原点会随书签栏显隐漂移。

### 2.4 Python 环境

`.venv` 里核心依赖和视觉依赖都装好了（pillow、pytesseract、mss、pywin32、pyautogui）。
系统 Python **没有**核心依赖，别用。

```bash
.venv/Scripts/python.exe -m pytest -q
```

Tesseract 在 `C:\Program Files\Tesseract-OCR\tesseract.exe`。

## 3. 游戏界面事实（实测）

### 3.1 窗口标题必须精确匹配

app 模式游戏窗口标题**恰好是 `EVO`**。本地控制台的页面标题是「情报中心 · EVO-Helper」，**含 `EVO`**。

按子串匹配会抓到控制台，把控制台的像素喂给游戏解析器，**而且表面上一切正常**。
用 `game_window.find_game_window()`，它做精确匹配。

⚠️ **但「加载中」不等于「没有」。** 页面重连时标题会临时退回 `eternal-void.online`
（游戏还没把 `document.title` 设成 `EVO`）。把它当成「窗口没了」去拉一个新的，
就会多出**第二个游戏窗口**——而 `find_game_window()` 一见到两个就拒绝工作，
扫描从此再也起不来，还得人工关窗口。实机上这个坑已经踩过一次。
`ensure_game_window()` 现在先查 `find_loading_game_window()`：已有窗口在加载就**等**
（上限 180s），绝不再拉一个；冷启动上限也从 30s 提到 120s。

### 3.1b 掉线恢复：入口页 → 进入 → START（已实机验证）

三屏的判据与按钮位置（截图 client 空间）：

| 屏 | 判据 ROI | 配方 | 按钮 |
|---|---|---|---|
| 游戏内 | 底部导航条文字 (800, 878, 1215, 908) | 3× | —— |
| 入口页 | 标题 (780, 305, 1140, 355) 读出 `ETERNAL VOID` | 3× | 进入 (999, 385, 1118, 430) |
| START | (820, 745, 1100, 830) 读出 `START` | 3× + **二值化阈值 180** | 同 ROI 中心 |

两条硬约束：

- **判入口页必须排在判 START 之前**。入口页是浮层，底下那个 START 页仍在画面里；
  反过来判就会在入口页上去点 START。
- **整屏 OCR 读不出 START**：那是压在星空上的半透明大字，psm 6/11/12 全军覆没。
  必须紧凑裁剪 + 二值化。
- 两个按钮都是**读到了才点**，读不到就停——判据来自这一屏本身，
  而不是「不在游戏里就多半是它」。

### 3.2 恒星系导航（截图坐标，client 空间）

| 元素 | 坐标 |
|---|---|
| 银河系输入 | (795, 71) |
| 恒星系输入 | (959, 71) |
| 行星输入 | (1122, 71) |
| OK | (1029, 132) |
| `«` 上一恒星系 | (735, 528) |
| `»` 下一恒星系 | (1192, 528) |

两条关键行为：

- **点任一输入框会弹出覆盖整条导航栏的编辑浮层**，所以一次只能设一个字段：
  点字段 → 输入 → 点 OK → 再设下一个。一口气点两个字段，第二个数字会进第一个字段。
- **`«` / `»` 是切换恒星系，不是翻页**。点三次恒星系从 137 → 140，**画面几乎不变但坐标变了**。
  只比画面差异会误判成「没生效」——必须查导航栏的坐标数字。

### 3.3 行星详情面板有两种布局

| 布局 | 特征 | 坐标 ROI | 名称 ROI |
|---|---|---|---|
| 无主（荒芜行星 / 敌对海盗） | 名字居中、坐标在下 | (840, 394, 1080, 430) | (840, 352, 1080, 392) |
| 有主 | 坐标/玩家/联盟 三行靠左上 | (830, 266, 1010, 292) | 玩家 (830, 288, 1080, 314) |

判别方法：先试「有主」坐标框，读出合法坐标就按有主解析，否则按无主。
**有主布局的名字在 `owner` 字段，无主在 `planet_name`**——只读后者会把所有 bot 漏掉（踩过）。

### 3.4 恒星系视图的快筛路线：已否决

恒星系视图里 bot 星球显示为 `bot_2_121_7's Planet`，**名字编码了坐标**，OCR 能干净读出，
未探索的行星显示「?」。原本设想据此做「快筛」：翻恒星系视图读名字找候选，只对候选点进去确认。

**实测否决。** 「一屏能看到几个行星位」这个问题本身不成立——恒星系视图是一张
**可平移的散点图**（拖动可露出更多行星），行星**没有位号标签**，靠近边缘的名字还会被裁掉
（`Silent Ho…`、`Iron Shro…`）。因此看不出「20 个位是不是都看到了」，**覆盖度无法自证**，
用它做主路径会留下静默数据缺口。主路径仍是逐坐标导航（7.2s/坐标，实测稳定）。

### 3.4b 游戏会自己回到行星视图

会话刷新时实测发生过：导航栏整条消失，画面回到自己星球。这时再往 `(795, 71)` 点
就是在认不出的画面上乱点。恢复路径：点底部左侧星球按钮 `(749, 856)` 弹出子菜单，
再点其中的恒星系 `(737, 748)`。`SystemNavigator.ensure_system_view()` 已封装。

**判据用导航栏标签，且要求三个命中两个**——`行星` 在底部导航条上也有，单个命中证明不了在哪一屏。

### 3.4c 每个恒星系恰好一个 bot（用户确认的游戏规则）

完整扫过的 111 个恒星系里，**每一个都恰好有一个 bot**，零例外；用户已确认这是游戏规则。
bot 的位号分布大致均匀（位 5–20 各 3–11 个）。

**扫描据此在找到 bot 后跳过该系剩余位**，期望 8.5 位而不是 16 位，全宇宙约 144 小时 → 76 小时。
`--scan-full-systems` 可关掉这条规则。

⚠️ 「本系已扫完」的判据因此**不是**「16 个位都入库」，而是「找到了 bot 或 16 个位都入库」。
这条判据只有一份（`systems_with_bot()`），主循环和 `--rescan-missing` 共用——
各写一份的话，补缺口会把主循环故意跳过的位当成缺口，每跑一次重扫一遍，永远补不完。

⚠️ 一开始看到的那个「例外」（2:9 零个 bot）其实是 OCR 把 `bot_2_9_5` 读成了 `botleao.-`，
连 `bot_` 前缀都糊了——**bot 判定就是看这个前缀，所以那颗星球作为普通空位入了库**。
这类失败不报错、不进缺口清单，是靠上面这条分布规律才发现的。已加两道处理：
`reread_owner()` 在名字「像 bot 但前缀不对」时换配方重读，
`--recheck-suspicious` 反查库里已有的这类行。

### 3.4d 攻击侦查链路（实机跑通，2026-08-08）

用**探路预设**打 bot 拿战报、记舰艇。这是用户定义的「模式 1」，与游戏内那个「侦察」按钮无关——
游戏内侦察是另一个任务类型（简报页任务类型图标里那个眼睛）。

七步，坐标为截图 client 空间：

| 步 | 动作 | 坐标 | 到达的画面 |
|---:|---|---|---|
| 1 | 导航到 bot 坐标 | `SystemNavigator.goto()` | 行星详情面板（有主布局） |
| 2 | 点「攻击」 | (820, 400) | 派遣界面，**起点与终点已自动填好** |
| 3 | 点「预设 »」展开 | (1176, 646) | 预设列表 `预设 2/10` |
| 4 | 点「探路」 | (984, 691) | 舰队变为 小型运输船 × 1 |
| 5 | 点绿色 ✓ | (1155, 762) | **简报页**（不是派遣！） |
| 6 | 核对 `任务类型: 攻击` | 见下 | —— |
| 7 | 点「出发！」 | (1077, 815) | 真派出，进「飞行中」 |

面板上另外三个按钮：攻击 (820, 400)、侦察 (911, 392)、扫描 (1004, 400，带禁用角标)、回收 (1097, 400)。

**绿色 ✓ 不是最后一步。** 它只是进简报页；真正派出去的是「出发！」。
把 ✓ 当成派遣会以为已经打出去了，实际停在简报页——反过来，
以为 ✓ 无害而随手点，也不会误派。简报页给出任务类型、飞行时间、预计到达、气体消耗，
是安全不变量「派攻击前简报必须写着攻击」唯一能核对的地方（`parse_dispatch_briefing`）。

实测一次：2:137:18 → 2:137:14（`bot_2_137_14`），飞行 8分48秒，气体 649.09M，
简报任务类型「攻击」，出发后出现在飞行中列表。

⚠️ **预设的身份是名字，不是舰种组成**（用户确认）。游戏预设面板：`海盗`（深空吞噬者 60 /
钛能守卫者 30）、`探路`（小型运输船 1）。项目里原先记的 `探路 = 轻型战斗机:1` **是错的**，
签名对不上会让 `workflow` 判定 preset 不匹配并安全暂停，也就是永远派不出去。已改为
`小型运输船:1`，并同步更新了 `scan_ranges` 里的存量数据。组成签名保留为**选中之后的复核**。

### 3.5 坐标空间

- 9 个银河系 × 499 个恒星系 = 4,491 个恒星系
- 每恒星系最大 **20** 个行星位
- **1–4 位恒为海盗**，可扫 5–20 共 16 位
- 全量 = 4,491 × 16 = **71,856 个坐标**

⚠️ `coordinates.POSITION_LIMIT = 499` 是**每银河系的恒星系数**，不是行星位数。
拿它当位数上限游标会空转 479 个不存在的位。位数上限用 `scan_bounds.MAX_POSITION`。

### 3.6 游戏内时间一律 UTC+0

与调度时区（用户的运行窗口，UTC+8）是两回事，不能混用。
见 `vision.parsers.GAME_DISPLAY_ZONE` 与 `domain.scheduling.SHANGHAI_OFFSET`。

## 4. OCR 配方（都是实测出来的）

| 目标 | 配方 |
|---|---|
| 坐标 | `lang=eng`, `--psm 7`, 白名单 `0123456789:[]`, **放大 5× 与 10× 各试一遍** |
| 行星名 / 玩家名 | `lang=chi_sim+eng`, `--psm 7`, 放大 4× |
| 舰队列 | **读两遍**：名称用 `chi_sim`、数量用 `chi_sim+eng`，按行合并 |
| 恒星系视图行星名 | `lang=eng`, `--psm 11`（字号大，反而更稳） |
| 底部导航条 | `lang=chi_sim`, `--psm 7`，紧凑裁剪 (790, 870, 1215, 905) |

几条硬经验：

- **混合语言下开头的 `2` 会被读成 `e`**（`[2:137:18]` → `[e:137:18]`）。数字白名单能解决。
- **坐标的冒号又细又矮，会被漏读**（`[2:122:9]` → `[2122:9]`）。所以核对坐标用**数字序列比对**
  而不是解析——我们不是在自由解析坐标，是在核对「面板显示的是否就是请求的那个」。
- **相邻的 `1` 会粘成一根竖条**——这是本轮最大的一类失败。游戏的数字字体把 `1`
  画成没有衬线的竖条，`2:111:11` 读成 `2:10:11`、`2:112:6` 读成 `2:12:6`、位 11 读成位 1。
  影响面：**位 11 是全宇宙的 1/16**，含 `11` 的恒星系（11、110–119、211、311、411）约 2.8%。
  重新导航再读没用（实测读三遍错在同一处），要换的是**配方**不是画面。两条对策：
  - **放大要用最近邻，不能只用 LANCZOS**。LANCZOS 会把两根竖条之间那道缝插值糊掉。
  - **白名单去掉方括号**：`]` 会被读成 `3`（`[2:91:9]` → `[2:91:39]`），而那个位上住着
    `bot_2_91_9`——丢的是 bot 本身。
  最终用四套配方 `(7,LANCZOS) (7,最近邻) (3,最近邻) (3,LANCZOS)` 逐套试，任一套核对通过就采信。
  172 张样本（含 63 张实机失败样本）上通过 140，救回 67 张原本失败的。
  **多档取或是安全的**：核对单向，读错凑不出请求串。
- ⚠️ **仍未解决**：`11x` 恒星系（尤其 112）四套配方都读不出，`2:112:6` 稳定读成 `2:12:6`。
  试过拿导航栏的恒星系输入框做旁证——那个框读得更差（读出 `2`、`11`、空），已放弃。
  这类坐标会被拒绝并记进 `var/logs/scan-gaps.txt`，**不会静默丢失**，可用
  `--rescan-missing` 复扫，但目前复扫大概率还是失败。占比约 2.8%。
- **面板可能还停在上一个坐标**（`2:123:5` 读出 `[2:123:4]`）。这不是读错是跳晚了，
  重新导航再读才有用。所以两条恢复路径都要有：换配方（读不出）与重导航（没跳过去）。
- **舰队名和数量不能一遍读**：`chi_sim+eng` 数量全对但名称掉拉丁噪声，`chi_sim` 名称只差一个字
  但数量会坏（`5` → `日`）。
- **bot 名后半段的数字会被读成字母**（`bot_2_1l21_7`、`bot_e_124_12`）。
  `ingest_scan.normalise_bot_name` 只在 `bot_` 前缀**之后**替换，前缀不碰（bot 判定就靠它）。
- **整屏 psm 6 在大片星空上很不稳**，尽量用紧凑裁剪 + psm 7。
- 底部导航条读成「和量 般队 太空能 商店 联盟」——只有 **商店、联盟** 稳定，判据只能用这两个。

### 4.1 战报数量：tesseract 在这套字体上走到头了

结论先写：**逐行读 + 合计校验 + 拖动重拍，都不足以读对战报里的舰队数量。**
下一步应改用**数字模板匹配**——前身项目早就撞过同一堵墙并这么解决了
（`CLAUDE.md`：「资源数字识别走 `templates/digits/` 模板匹配，tesseract 是备选」，
现成模板在 `templates/digits/`）。

走到这一步的过程与四个结论，别重走：

1. **整列一次读会切错行**。中文字形旁边 tesseract 分不准行，`11` 被吞成 `1`、
   `39` 被切成 `33`。改逐行读之后，**每一行的候选集里都含真值**。
2. **行位置要用「带顶 + 中位行距」**，不能用检测带的中心。最后一行的检测带常常过高
   （尾随空白并进来），按中心裁会整行落空——`17` 一直读成空就是这么来的。
3. **合计校验单独不够**。实测这份战报：候选组合共 1944 种，合计等于 247 的有 **28 种**，
   而真值按得票排只排第 **5**。得票最高那组是 `(117, 41, 13, 1, ...)`——
   `41` 多的 10 与 `11→1` 少的 10 正好抵消。**按「合计对上 + 得票最高」交付会稳定选错。**
4. **拿「第1回合剩余战舰」交叉验证也不行**。bot 损失 0，两节本应逐行相等，
   但 `31→41`、`11→1` 在两节里**同样发生**——是字形层面的系统性误读，不是随机噪声，
   独立成像也消不掉。何况回合节常被截断（实测只看得到 7 行）。

已经建好并保留的部分：分节按横幅定位（`banner_bands` / `sections_from_banners`）、
逐行定位、`fleet_counts.read_until_total` 的「对不上就不入库」判据。
判据要留着——它正是挡住那 28 组假解的东西。

## 5. 下一步任务

### 5.1 扫描工具已正式化（本轮完成）

`tools/scan_coordinates.py`，四块都接上了：`scan_plan`（顺序 + 跳过海盗位）、
`ensure_game_window`、`SessionKeeper`、`run_instances` 持久化游标。

```bash
.venv/Scripts/python.exe -m evo_helper.tools.scan_coordinates --status      # 只看计划与游标
.venv/Scripts/python.exe -m evo_helper.tools.scan_coordinates --limit 200   # 扫 200 个就停
.venv/Scripts/python.exe -m evo_helper.tools.scan_coordinates               # 一直扫
```

运行实例幂等键 `priority-scan-0001`，**重跑即续扫**；游标只在坐标读完落库后才前进，
所以中断最多重扫一个坐标。已确认过的坐标默认跳过（`--no-skip-scanned` 可强制复查）。

扫描顺序（已定，有测试固定）：

| 序 | 分段 | 坐标数 | 累计 |
|---:|---|---:|---:|
| 1 | 2:001–200 | 3,200 | 3,200 |
| 2 | 2:201–499 | 4,784 | 7,984 |
| 3 | 1:001–499 | 7,984 | 15,968 |
| 4–9 | 3、4、5、6、7、8 系 | 各 7,984 | 63,872 |
| 10 | 9:001–499（补） | 7,984 | 71,856 |

> **9 系需要用户确认**：用户给的顺序是「1345678」，没有 9 系。
> 当前实现把它补在最后而不是丢掉——「优先」不能悄悄变成「只扫」，否则会产生看不见的数据缺口。

耗时：逐坐标 7.2s/个（本轮实测稳态）→ 第一段约 6.4 小时，全量约 144 小时。
「一屏读整系」的快筛已在 3.4 节否决，没有更快的已验证路线；缩短总时长只能靠拆分多次续扫。

### 5.2 侦查按 bot 坐标输入

用户要求：「侦查只要输入 bot 坐标即可」。

现在数据库里已有 5 个 bot 坐标，检索接口也在，缺的是「给定坐标 → 执行侦查」的动作链路。
行星详情面板上有 **侦察** 按钮（有主布局，位于 攻击 右侧）。

⚠️ 这条链路会真正操作游戏，必须走 ActionGuard，并且 `FORBIDDEN_LABELS` 目前会拦截
「攻击/派遣/删除/领取」——侦察是否要放行、放行到什么程度，需要先和用户确认。

## 6. 安全不变量（不要破坏）

- `dry_run` 默认 true，界面上**没有开关**；解锁只能走单目标 Canary 流程
- `pyautogui.FAILSAFE = True` 必须保持（鼠标甩到屏幕左上角急停）
- 所有点击走 `HumanInput`（随机偏移/移动时长/间隔）。动作闸门 `allow_actions` **默认关**，
  关着时 `FORBIDDEN_LABELS` 里的词一律拒点；只有攻击侦查链路在构造时显式打开。
  **不要靠从黑名单里删词来放行**——删了词，扫描器也就一并获得了点攻击的能力。
- **认不出的画面绝不点击**——可能是维护公告或弹窗，乱点会误触派遣/删信/领奖
- **不与用户抢登录**：重连失败按 `SessionBackoff` 退避，退避耗尽安全暂停
- 派攻击前简报必须写着「攻击」（`parse_dispatch_briefing(...).is_attack`）
- 坐标必须「请求 vs 面板读回」双向一致才入库

## 7. 踩过的坑（别重复）

1. **`find_window("EVO")` 抓到本地控制台**——控制台标题含 EVO。用精确匹配。
2. **最大化窗口 `SetWindowPos` 无效**——先 `SW_SHOWNORMAL` 还原。
3. **未声明 DPI 感知**——125% 缩放下窗口尺寸全是逻辑像素，怎么调都不对。
4. **只比画面差异不查坐标**——误判 `»` 翻页无效，实际它在切恒星系。
5. **入库只读 `planet_name`**——有主布局的名字在 `owner`，5 个 bot 全被当成空位漏掉。
6. **校验规则在 Fake 和持久化服务里各有一份**——只改一处，浏览器实测仍然报错。
   改任何 service 层规则时**两处都要看**。
7. **alembic `fileConfig` 默认 `disable_existing_loggers=True`**——Web 运行时启动就迁移，
   会把应用日志静音一整个进程。已修为 `False`。
8. **全屏 mss 抓屏会拍到用户私人窗口**——已排除该方案，只截指定窗口。
9. **离线回归只断言数量不断言名称**——放过了 `无畏舰`→`AKER` 等 4 处名称错乱。
   名称是舰队时间线做差异的键，错了会让每份报告都显示成「首次出现」。
10. **固定等待代替轮询**——切屏时间不稳定，死等常常还停在上一屏。
11. **点击落到别的窗口上**——游戏窗口不在前台时，`pyautogui` 的点击打在盖住它的窗口上，
    而截图（`PrintWindow`）照样是游戏画面，于是「点了没反应」看起来像游戏没响应。
    点击前必须真的抢到前台。**光调 `SetForegroundWindow` 不够**：Windows 只允许当前
    拥有前台的那个线程换前台，直接调会被拒，抛出的异常里 `GetLastError` 还是 0
    （`pywintypes.error: (0, 'SetForegroundWindow', 'No error message is available')`）。
    要先 `AttachThreadInput` 把自己挂到前台线程上，然后**回读 `GetForegroundWindow` 核实**，
    并容忍间歇性失败退避重试——用户在别的窗口打字时被拒是常态，不是故障。
12. **会话巡检裁剪连图标一起裁**——同一屏两次读出 `区 Y ASA 商店 o j` 和 `6S BOD ®@`，
    前者恰好含「商店」后者不含，在线的会话被**随机**判成「认不出」。只裁文字那一行。
13. **`claim_next_coordinate` 拿 `POSITION_LIMIT`（499）当行星位上限**——那是每银河系的
    恒星系数。游标会在每个恒星系里空转 479 个不存在的位，看起来一直在跑却一个新坐标都没到。
    已改为取区间自己的 `start_position` / `end_position`。

## 8. 常用命令

```bash
# 测试与检查
.venv/Scripts/python.exe -m pytest -q
.venv/Scripts/ruff.exe check src tests
.venv/Scripts/mypy.exe src

# 起本地控制台（127.0.0.1:8000）
.venv/Scripts/python.exe -c "from evo_helper.web.runtime import main; main()"

# 战报入库
.venv/Scripts/python.exe -m evo_helper.tools.ingest_report --detail <图> --replay <图>

# 坐标扫描（续扫；--status 只看进度不动游戏）
.venv/Scripts/python.exe -m evo_helper.tools.scan_coordinates --status
.venv/Scripts/python.exe -m evo_helper.tools.scan_coordinates

# 桌面状态窗：调度器的瘦客户端。右下角显示「现在跑的是哪条链路 + 跑了多久」，
# Alt+F8 开始调度 / Alt+F9 结束调度（等同网页上的开始/结束）。
# **它自己不起任何进程**，得先把上面那台 Web 服务跑起来，否则只显示「未连接」。
.venv/Scripts/python.exe -m evo_helper.tools.scan_console
.venv/Scripts/python.exe -m evo_helper.tools.scan_console --stop-key ctrl+alt+f9

# 旧的扫描结果入库（外部 JSON）
.venv/Scripts/python.exe -m evo_helper.tools.ingest_scan --results <json> --run-id <uuid>
```

日志在 `var/logs/report-read.log`（2MB 轮转）、`var/logs/scan-priority.log`（扫描）。
数据库 `var/evo-helper.db`。

> 扫描期间会**反复把游戏窗口提到前台并操作鼠标**，机器在此期间不能同时用于别的事。
> 急停：把鼠标甩到屏幕左上角（`pyautogui.FAILSAFE`）。

⚠️ **这台机器上 `Alt+F9` 与 `Alt+F10` 被 NVIDIA ShadowPlay 占用**
（`RegisterHotKey` 返回错误码 1409）。控制台会逐个注册、逐个报告，
能用的照常生效；换键用 `--stop-key ctrl+alt+f9`（实测空着），
或去 GeForce Experience 里把录制快捷键改掉。状态窗右键=结束调度，不依赖任何注册。
（右键**只停不启**：做成切换的话，在状态刚变过的那一瞬右键会变成又起一轮。）
