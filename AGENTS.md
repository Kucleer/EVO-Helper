# AGENTS.md —— 多个 AI 助手共用的工作记忆

**这份文件是给「下一个接手的 AI」看的。** 它假设你**没有任何会话上下文**：
你没参与过之前的讨论，不知道哪些结论是实测的、哪些是猜的，也不知道哪些坑已经踩过。

登记 2026-08-21。用户口径：

> 因为周额度用完了，我会启动其他 AI agent 来处理。我需要你准备一个 md 文件来保存
> 你的记忆，可能会有多个不同的 AI agent 来共同协作以及维护这个文件。你来规定好规则。

---

## 〇、先读这里

**这份文件就是入口。** 按顺序读第一节（维护规则）→ 第三节（当前状态）→ 第五节（踩过的坑）。

⚠️ **本文件不是需求文档，也不是设计文档。** 那些在 `docs/` 下，本文件只做索引和状态。

### 0.1 ⚠️ `CLAUDE.md` 不在仓库里

根目录那份 `CLAUDE.md` 被 `.gitignore` 挡着（第 63 行 `/CLAUDE.md`），**只存在于用户
本地**。Claude Code 会自动加载它，**别的 AI 助手看不到**。

⇒ **所以本文件必须自带硬约束**（第五节），不能只写「见 CLAUDE.md」。
⇒ 如果你是 Claude Code，你会同时看到两份；**冲突时以 `CLAUDE.md` 为准**，并顺手来
   本文件改掉不一致的地方。

### 0.2 这是**唯一**的记忆文件

2026-08-21 删掉了另外三份交接文档，因为它们各自过期、互相引用、而且和代码里的注释
重复——**多份「当前状态」必然漂移**，那正是规则 1.3 要防的事。

| 删掉的 | 写于 | 为什么删 |
|---|---|---|
| `HANDOFF.md` | 2026-08-08 | 说 `main` 在 `78150af`、494 个测试（实际 3,701）。它的「OCR 配方」「界面事实」**代码里有更新更详的版本**（`vision/resource_digits.py` 记着 45 套配方的对比，`game/pirate_ui.py` 是活的 ROI 常量） |
| `TODO-交接.md` | 2026-08-11 | 说 1,026 个测试。有用的只剩「四条闸门」那段，已并入下面 0.3 |
| `SUBAGENT_IMPLEMENTATION.md` | 2026-08-11 | ⚠️ 写着「目标仓库 …（**Private**）」，而**仓库是公开的**——照它的假设提交截图会泄露坐标与账号信息 |

⚠️ **它们仍在 git 历史里**：`git show 6a36448:HANDOFF.md` 之类可以取回。
删的是「现役文档」这个身份，不是内容。

### 0.3 验证基线（四条闸门，CI 就跑这四条）

```bash
python -m pytest tests -q
python -m ruff check src tests
python -m ruff format --check src tests
python -m mypy src
```

⚠️ **范围必须是 `src tests`，不是仓库根。**
⚠️ **`mypy` 不带参数不能当基线**：`[tool.mypy] packages = ["evo_helper"]` 会让它去
site-packages 找包，撞上 `py.typed` 缺失而整个跳过。**一律 `mypy src`。**
⚠️ 本机 mypy 版本若不在 `pyproject` 声明的 `>=1.11,<2` 内，**它报的结果不作数**——
以 CI 为准。真要修依赖先 `uv sync`，**别带 `--all-extras`**（装上 `ultralytics` 会引入假错）。

⚠️ **本机有两个解释器，mypy 结论相反**（2026-08-22 实测，害人不浅）：

| 跑法 | mypy | 结果 |
|---|---|---|
| `python -m mypy src`（裸 `python` = 3.14 32-bit） | **1.14.1，在声明范围内** | 3 errors ← **这个才作数** |
| `.venv\Scripts\python.exe -m mypy src` | 2.3.0，**超出 `<2`** | `Success: no issues` |

⇒ **一律用裸 `python -m mypy src`**。拿 venv 那个跑会把下面这 3 条既有错误判成通过：
`domain/intel_query.py:88`（no-untyped-call）、`application/mission_scheduler.py:2973`
（两条 union-attr，`min(..., default=None)`）。**这 3 条在 `main` 上就有**（PR #234 时已逐字比对过），
所以验收判据是「**不新增**」，不是「全绿」。

### 0.4 ⚠️ 实测出来的事实**住在代码注释里**，不在文档里

这个仓的注释密度很高，而且**注释才是活的**：OCR 配方、界面 ROI、飞行时间标定、
每一处判据「为什么是这个值」，都写在它旁边。

⇒ **要改一个常量之前，先读它的注释**。多数常量都写明了它是「运维旋钮」还是
「标定常量」，以及调错的后果。
⇒ **不要把这些抄进本文件**——抄一份就是造第二份真相。

## 一、⚠️ 维护规则（用户点名要求的部分）

### 1.1 写「事实」必须带三样：**数值、样本量、日期**

```
✅  稀有 ≈ 0.141 × 军力（实测 n=74，2026-08-18~19，R²=0.781）
❌  军力和收益正相关
```

没有样本量的数字，下一个人无法判断它值不值得信，只能重新做一遍。

### 1.2 ⚠️ 每条结论必须标**证据强度**，四选一

| 标签 | 含义 |
|---|---|
| **实测** | 从生产库或实机量出来的，附样本量 |
| **推断** | 从实测推的，中间有一步没验证 |
| **用户口径** | 用户说的领域知识。⚠️ **不要去验证它，也不要质疑它**——用户比代码更懂这个游戏 |
| **直觉** | 没有数据。⚠️ **必须标出来**，否则下一个人会当事实用 |

**这条是踩出来的**：`docs/周内相位-策略模型.md` 整份是「直觉」，标注清楚之后才没被当成实测。

### 1.3 ⚠️ 推翻旧结论时，**改原处 + 记在第五节**，绝不留两份矛盾的

2026-08-20 就犯过：在《军力被打败会崩-实测》里收回了「指数形式错了」这个说法，
却忘了改 `docs/AI选靶-方案.md` 2.7 表里的同一句，于是两份文档互相打架。
**而那份方案是准备喂给 LLM 的——自相矛盾比说错更糟。**

**做法**：搜一遍这个结论出现在哪几个文件（`grep`），全部改掉，再在第五节写一行。

### 1.4 ⚠️ 拟合出来的系数，**取值范围必须和使用范围对齐**

**这条是最贵的一次教训**：`exp(−0.068×龄)` 这个衰减系数在 0–22 小时上拟得很好
（R²=0.781），但系统只在 **0–6 小时**窗口里工作，而定出那个系数的 **5 个样本全在
窗口之外**。窗口内单独重拟，最优衰减是 **0**，硬套 −0.068 比取平均还差。

⇒ **报一个拟合结果时，必须说清它在哪个区间上拟的、将被用在哪个区间。**

### 1.5 「不知道」要写出来，不许留白

每次交接必须回答三个问题（第六节的模板）：

- 做了什么
- **什么没做完 / 卡在哪**
- **⚠️ 什么是你没验证的**

⚠️ **第三条最重要。** 上一个人没验证的东西，下一个人会默认它验证过。

### 1.6 冲突以什么为准

```
生产库里的数据  >  用户口径  >  代码  >  本文件  >  docs/
```

⚠️ **本文件可能过期。** 发现它和库里的数据对不上，**以库为准，并顺手改掉本文件**。

### 1.7 并发：多个 agent 同时改这份文件

- **第六节的交接日志只 append，写在最上面（倒序）**，冲突面最小；
- **每条署名 + 日期**（`2026-08-21 / Claude` 这种，用你自己的标识）；
- **改第三节（当前状态）之前先重读一遍**，别覆盖别人刚写的；
- **不许删别人写的条目**。要推翻就标 `~~已推翻~~` 并写清理由和日期。

### 1.8 篇幅上限：**500 行**

超了就把**已经落地、不再需要每次读**的内容移进 `docs/` 下的专门文档，
本文件只留一行索引。⚠️ **一份没人读得完的记忆等于没有记忆。**

---

## 二、这个项目在做什么（30 秒版）

浏览器游戏 **Eternal Void** 的**完全外部**自动化：截屏 → 模板匹配/OCR → 真实鼠标点击。
不碰 DOM、不碰协议。两条独立流水线 + 一个 Web 控制台。

**当前的核心目标**：用有限的**航线**（同时在飞的舰队数，现在 9 条）去打 bot 星球，
最大化**核心三资源**（合金碎片/泰坦立方/收割者碎片）的产出。

**唯一的目标函数**：**每航线小时的稀有三样产出**。航线是瓶颈（实测利用率一度 99.2%），
所以「一发能拿多少」不是判据，「一条航线一小时能换多少」才是。

---

## 三、当前状态（2026-08-21 07:30 UTC+8）

### 3.1 代码与部署

```
main            6a36448
迁移 head       a3c81f5d2b64   （单 head）
生产库          a3c81f5d2b64   ← 已同步
开着的 PR       0
```

⚠️ **生产跑在另一台机器**：`CY-202305011401`，`E:\Kucleer_code\EVO\EVO-Helper`。
控制台启动时会往 `system_log` 写一行代码版本（commit / branch / dirty / 迁移前后 revision）。
**要判断生产跑的是哪个版本，查那一行**，不要从「PR 合了」去推。
⚠️ 最近一次记录 `dirty: true` —— 那台机器在 `main` 之上还有**未提交的本地改动**，
内容不可见。有查不通的现象先看这里。

### 3.2 出发点配置（2026-08-20 深夜刚转场）

| 任务 | 出发点 | 航线 |
|---|---|---|
| task#2 一号攻击 | `5:261:8` | 4 |
| task#6 二号攻击 | `8:117:6` | 3 |
| task#8 三号攻击 | `7:228:15` | 2 |

⚠️ **task#2 的扫描范围参数可能还配着老的 `galaxy 4 / 227–327`**，转场后需核对。

### 3.3 在飞的工作

- **战报补录按预期时刻检索**：已合并到本地 `main`（`18eb66f`），尚未推送远程；
  详见第六节最新日志。

### 3.4 挂着没做的

| 事项 | 卡在哪 |
|---|---|
| AI 选靶一期（影子模式）已上线但**开关默认关** | 两个已知缺陷未修：`AI_MAX_TOKENS=2000` 对推理模型不够（拿到空串）、缺 `reasoning_effort`。用户要先手动调 prompt |
| 衰减项进排序 | **刻意不做**，理由见 `docs/选靶数据跟踪-待办.md` 第八节 |
| 军力历史表 | 用户 2026-08-20 判断**不必做**（攻击日志已够拟收益公式） |
| `_beat_uptime()` 落库失败不限流 | 持续失败会每秒写一条日志（一天 86,400 行）。已发现未修 |

---

## 四、已确认的事实（附证据强度）

### 4.1 【用户口径】核心三资源 = 打爆的舰艇残骸

⇒ `稀有 ≈ 0.141 × 军力` **不是相关性而是恒等式**，0.141 是转换率。
⇒ **军力读数的语义是「剩余可收获库存」**，不是「有多难打」。
⇒ 排序键 `军力 ÷ 往返小时` 字面上就是「库存 ÷ 取货时间」。

### 4.2 【用户口径 + 实测】军力周内只降不升，周一 UTC+0 刷新

被打败会当场掉 **20–93%**（实测 42 条：27 不变 / 14 降），本周期内不再恢复。
⇒ **读数永远是真实军力的上界**。
⚠️ 旧文档里「一周内基线不动」是**错的**，已更正。

### 4.3 【实测】转场到近距离出发点，每航线小时翻倍

```
转场前（4:277:15 + 9:250:8，往返 119/111 分）   1,009 /线小时
转场后（5:261:8 + 8:117:6，往返  49/60 分）    2,045 /线小时    +103%
```

⚠️ **收益里有两部分，性质不同**：往返时长是**位置的永久属性**，打不掉；
「这块地还没被打过」是**临时**的，几天就没。

### 4.4 【实测 → 因果链】本地目标为什么会枯竭

```
高强度攻击 → 打败后军力掉 20–93% → 掉出全服榜前列
→ 军力榜按军力降序只采前 N，再也读不到 → 被 score_max_age_hours 窗口筛掉
→ 候选池只剩跨银河 → 往返变长 → 每航线小时下降
```

⚠️ **这是自我收紧的反馈环，调大 `top_n` 治不了本。** 能打破它的只有
**换出发点**或**等周一刷新**。

### 4.5 【实测】两种保护期，代价差两个数量级

| | 触发点 | 代价 |
|---|---|---|
| 派遣时弹「没有可执行的任务」 | 舰队没起飞 | ~38 秒鼠标时间 |
| **到达才发现（邮件「处于保护状态，我方舰队已返航」）** | 飞完全程 | **一整趟往返的航线** |

⚠️ 近 5 天 **84 发派遣从来没有战报 = 21.1%**，合计白占 **122.8 航线小时**。
**只确认了其中 2 发是保护期**，其余可能是 OCR 失败或还没翻到——**不要把 84 发全算成保护期**。

### 4.6 【实测】基础三样（金属/晶体/气体）由**我方货舱容量**决定，与目标无关

AAA 预设那 6 条的变异系数是 **0.0001**，军力跨 10,020–11,170 纹丝不动。
⚠️ **绝不许进任何「军力→收益」模型**，放进去会让「预设大的星球」无脑领先。

### 4.7 游戏内显示的时刻**就是 UTC**

已用已入库战报逐秒核对（邮件 `20/08/2026 19:22:08` ↔ 库里 `19:22:08 UTC`）。
控制台页面上标的「游戏时间 UTC+0 · 现实时间 UTC+8」是对的。

### 4.8 【用户口径】`FIRST_BOT_RANK = 587` 那个「bot 起点」**不是真 bot**

用户口径（2026-08-22）：「bot 的起点目前是**玩家改名伪装**的 bot，并不是真正的 bot」。

⇒ 判据只看名字前缀（`domain.ranking.mentions_bot`），改名成 `bot_x_y_z` 的真人一样命中，
   所以那个 587 是**被伪装污染的观测值**，真 bot 区在更后面。
⇒ **不许拿 587 当盲滚的安全边界**去拦、去标红、去算余量——拿一个假边界报警比不报更坏。
   `game/ranking_ui.py` 的 `FIRST_BOT_RANK` 与 `BLIND_SCROLLS` 注释里那套
   「40×12=480 < 587 所以怎么都到不了 bot 区」的推理，**前提已不成立**。
⇒ 盲滚行数由用户在攻击配置页上定（默认 700），助手不做「越界」判断。

---

## 五、⚠️ 踩过的坑（每一条都出过事，别重蹈）

### 5.0 ⚠️ 硬约束（原本写在 `CLAUDE.md` 里，但那份不在仓库，所以这里必须重复一遍）

- **不许直接改生产数据库。** 调试、验证、跑数一律用**测试库** `evo_helper_test`
  （与生产同一台 PostgreSQL，连接串见 `.env`）。**动生产库需要用户当次授权。**
  用户口径（2026-08-17）：「除非授权实机验证，或者我特殊授权，不可以直接修改生产库」。
- **表结构变更只由生产自己升。** `web/runtime.py` 的 `_upgrade_database()`
  （启动时 `alembic upgrade head`）**就是部署机制，不要动它**。迁移合进 `main`、
  用户重启 `start-console.bat`、生产自己升。**开发一侧一次都不许跑 `alembic upgrade`。**
- **新功能必须带够用的日志。** 判据不是「有没有打日志」，而是**出事时能不能只靠库里
  的日志定位**。走 `infrastructure.system_log.record_system_log`，**落库不落文件**
  （实机在另一台机器，本地 `var/logs` 跨机取不到）。
  ⚠️ **每 tick 可能触发的要限流**，只在状态变化时写。
- **引入阈值先判断该不该可配置。** 分两类：**运维旋钮**（取值取决于用户处境，没有唯一
  正确答案）要可配置并写清调大调小的代价；**标定常量**（由屏幕几何、OCR 配方决定，
  调了就是错）不但不配置，还要在注释里写明「这不是偏好项」。
  判据：**改这个值会让结果变「更适合我」还是变「错」？**
- **坐标全部是全屏绝对像素**，前提是浏览器最大化 1920×1080、系统缩放 100%、窗口不动。
- **安全不变量**：`expedition_reports.py` 必须保持只读；`evo_bot.py` 的 `AUTO_ENABLED`
  默认 False。
- **反行为检测**：所有点击必须走拟人化路径（`human_click()`），不要引入瞬移或固定节奏。
- **紧急停止**依赖 `pyautogui.FAILSAFE = True`（鼠标甩左上角），不要关掉。
- **可选依赖缺失时降级运行而非报错**（plyer / pytesseract / pygetwindow / requests），
  新代码保持这一模式。
- **代码注释与文档一律中文**，源文件带 `# -*- coding: utf-8 -*-`，读写文件一律 UTF-8。
- **默认派子 agent 并行推进，不要全部 inline 做完。** 用户口径（2026-08-22）：
  「本会话需要经常调用子 agent」。多任务、可并行、或需要大量读文件的活儿一律派出去，
  主线程只留编排与判断。
  ⚠️ **每个子任务的提示词里必须复述这几条硬约束**（它们看不到本文件）：
  不连生产库、不对生产库跑 `alembic upgrade`、不启动游戏、不动鼠标、
  验证只跑 `pytest tests -q` / `ruff check src tests` / `ruff format --check src tests` / `mypy src`。
- ⚠️ **用户口径 = 领域知识，不要验证也不要质疑**（规则 1.2）。用户比代码更懂这个游戏；
  代码注释和实测数据与用户结论冲突时，**以用户结论为准**（规则 1.6 的优先级）。
  2026-08-22 犯过：用户定盲滚默认 700 行，助手拿代码里的 `FIRST_BOT_RANK`(587)
  两次提「越界」异议——而那个 587 本身就是被伪装 bot 污染的观测值（见 4.8）。

### 5.1 工具与流程

- **`SET default_transaction_read_only` 不生效**——它只对**之后开始的**事务管用，
  而 SQLAlchemy 的 Session 在执行它时事务已经开了。
  ⇒ **用 `SET TRANSACTION READ ONLY`**（对当前事务立即生效，必须是本事务第一条语句），
  并**实测一次写被拒**。
- **不许 `git stash`**：`.git` 在多个 worktree 间共享，曾有 agent 弹出了别人的 stash，
  造成 20+ 冲突。
- **不许起 preview / dev server，不许碰主工作区的 `.claude/launch.json`**：
  曾有 agent 因此**误起真控制台、把用户正在跑的那个顶掉**。
- **仓库是公开的**：提交前 `git ls-files | grep -iE '\.(png|jpe?g|webp)$'` 必须为空；
  ⚠️ **用例夹具里不许出现真实坐标**。
- **绝不对任何库跑 `alembic upgrade`**：迁移合进 `main`、由用户重启 bat 让生产自己升。
  **生产库的版本永远不该跑在 `main` 前面**（曾因此让 `main` 的代码启动时
  `Can't locate revision`，跑调度那台机器直接起不来）。

### 5.2 分析与判断

- **⚠️ 用有缺陷的搜索得出「东西不存在」的结论** —— 犯过两次。一次 `grep` 写错说
  「main 有两个 alembic head」，一次 `LIKE '%commit%'` 搜不到启动日志就断言「功能没写」，
  **而那条日志一直在**（消息里根本没有 "commit" 这个词）。
  ⇒ **搜不到时，先怀疑自己的搜索式。**
- **⚠️ 没验证就下结论** —— 「回收率 38% 所以少算」「跨 UTC 日所以对不上」，两个都是
  编出来的解释，查完全错。⇒ **先查再说。**
- **⚠️ 指标的分档太粗会藏掉主因** —— 用「≤90 分内的 Σ军力」给出发点选址，把
  `7:228:15` 排到倒数，而它实测是第一名的 3.9 倍。因为价值是 `军力 ÷ 往返`，
  而 42 分和 84 分被算成了同一档。
- **⚠️ 模型给的是瞬时上界，不是日均** —— 预测转场 +156%，实测 +103%。
  模型只取速率最高的前 10 个 (球, 目标) 对，那是**此刻最好**，不是**一天能持续跑出来的**。
  对没动过的 `7:228:15` 也高估 31%。⇒ **报预测时说清它是上界。**

### 5.3 用户明确否掉的做法

- **不考虑单位数量**（我方与敌方都不考虑）。
- **不要「打过所以打折」** —— 军力读数已经包含了这个信息，再修正是双重计数。
- **盲拖余量的告警不要再提** —— 用户配的没问题。
- **不要动用户的控制台配置** —— 优先级/范围是用户的，发现不符只说不改。
- **工作时间不许起游戏**（工作日 UTC+8 工作时间），实机需当次授权。

---

## 六、交接日志

### 2026-08-21 / Codex

**做了**：完成「战报补录按预期时刻检索」的代码与单测。默认对账模式会用当前待补
派遣的 `expected_report_at_utc` 合并为开信窗口：读全秒数为 `[-10s, +10s]`，飞行时长
恰为整分钟（OCR 可能丢秒）为 `[-10s, +70s]`。列表行时间 OCR 读不出、候选集为空、
或任一待补派遣没有预期时刻时，都保守地保留原有顺序开信行为；时刻只预筛「开不开」，
战报归属仍由详情坐标判据决定。每趟向 `system_log` 写一条汇总，包含区间数、扫描行数、
按时刻开封 / 盲开 / 筛掉数、认领数及未补派遣的预期时刻。

**验证**：`python -m pytest tests -q` 通过（3705 passed, 263 skipped）；`ruff check` 与
`ruff format --check` 通过。`python -m mypy src` 仍被既有的
`src/evo_helper/domain/intel_query.py:88` 未类型化调用拦住，非本次修改引入。

**没做完 / ⚠️ 未验证**：尚未在实机跑一趟补录，需观察该条 `system_log` 的
`skipped_by_time` 与 `still_missing`；没有生产库写入或迁移。实现提交 `0a38040` 已由
合并提交 `18eb66f` 并入本地 `main`，尚未推送远程或创建 PR。

### 2026-08-21（下午～傍晚）· Claude Code

**⚠️ 本次会话因额度耗尽切换，下面是没做完的和刚定下的。**

**合并了 #237 / #238**（AGENTS.md 收敛、到达时保护期邮件）。当前 `main` = `400ce0e`
之后又有合并，迁移 head `a3c81f5d2b64`，生产库同版本，**0 个 PR 开着**。

**根目录 md 收敛完成**：删掉 `HANDOFF.md` / `TODO-交接.md` /
`SUBAGENT_IMPLEMENTATION.md`（理由见 0.2），只剩 `AGENTS.md` / `CHANGELOG.md` /
`README.md`。⚠️ 内容都在 git 历史里。

#### 新写的方案（未开发）

**[`战报补录-按预期时刻检索-方案.md`](docs/战报补录-按预期时刻检索-方案.md)** ——
待办已登记在 [`待办-已登记未开发.md`](docs/待办-已登记未开发.md)。
⚠️ 用户提的 ±5 秒**实测只覆盖 97.7%**，方案里改成了非对称分档，理由在文档第一节。

#### 这一轮量出来的、值得当事实用的

| 结论 | 证据 |
|---|---|
| **转场后每线小时 +103%** | 08-20 补录后：转场前 1,009 → 转场后 2,045 /线小时 |
| **7 系反超不是「衰减更慢」** | 拆开看：目标价值只高 13%，其余全是**回收率差**（100% vs 85–89%）造成的假象；短程在半天窗口里天然显得好看 |
| ⚠️ **给选靶加「命中率」项只值 +2.6%** | 周一 3 小时推演：现有键 197,563 vs 加命中率 202,785。**不值得改代码** |
| ⚠️ **「只抢榜前 50」比让算法自己选差 16%** | 同一推演 166,848。`÷往返` 已经把远的惩罚掉了——用户原话：「6000 一小时路程就是不如 4000 半小时路程」 |
| **战报时刻误差分两族** | 见上面那份方案 |

#### 用户当天给的领域知识（都进不了代码，但改变了模型）

- **bot 是均布的，我方舰艇也是均布的** ⇒ 选址的「哪颗球库存大」大半是**当前被打空的程度**，不是站点固有属性
- **周一第一小时的未命中 = 飞到才发现 = 整个往返白飞**；之后才是派遣时就发现（38 秒）
- **周一计划：5 球 × 2 线**

#### 在用户手上的

1. **周一（08-24 UTC+0）刷新前**把舰队摆成 5 球 × 2 线
2. **周一临时调 `bot_limit` 到 30–50**（现在 500）——军力榜按军力降序，采够即交棒，
   压缩「刷新→第一发」的时间；头 2–3 小时后调回
3. **08-20 还缺 3 发战报**：`8:431:20` / `8:223:19` / `8:70:11`
4. **#238 那封保护期邮件的主题行原文**——判据建在正文上，
   ⚠️ 若列表行只显示主题且主题含「战报」二字，功能会静默失效

#### ⚠️ 我这一轮犯的错，写下来免得重犯

- **拿有缺陷的搜索得出「东西不存在」**：用 `message LIKE '%commit%'` 找启动日志没找到，
  就说「没写」——实际那条日志在，只是消息里写的是「控制台启动」。**同一天犯了两次。**
- **建议重启 bat 前没查生产状态**，用户已经重启过了。规则见 0.5。
- **选址判据一度从「速率」退回「存量」**，把 7 系排到倒数，而它实测是第一。
  ⚠️ 判据必须是 `军力 ÷ 往返`，**分档聚合会把 42 分和 84 分的目标算成一样**。
（**只在最上面追加，不要改动已有条目**）

### 2026-08-21 / Claude

**做了**：手工补录 08-20 的 2 份战报（`8:15:13` / `7:204:16`）+ 2 条保护期记录
（`8:485:17` / `9:189:11`）——用户当次授权写生产库。槽位映射用已入库的 `8:69:19`
逐格校准过（不是猜的）。补录后重算转场效果：**+103%**（此前测 +66%，差在战报没回全）。

**没做完**：08-20 还缺 3 发战报（`8:431:20` / `8:223:19` / `8:70:11`），
用户信箱里若还有邮件可以继续补。

**⚠️ 没验证的**：
- 那 84 发无战报的派遣里，只确认 2 发是保护期，其余成因不明；
- 5/8 系被打一整天之后的衰减速度——**这是判断「这次转场能撑几天」的关键**，
  今天（08-21）跑满一天之后才有第一个数据点；
- 生产那台机器 `dirty: true` 的本地改动内容。

---

## 七、给下一个 agent 的三条建议

1. **先看 `docs/选靶数据跟踪-待办.md`** —— 那是一张「等数据」的清单，
   每条写明了今天的结论、靠多少样本、要到多少才够下决定。**别重新做已经做过的分析。**
2. **改动前先量一次现状。** 这个项目里几乎每个「显而易见」的改进，量完之后都比预期小
   （排序层的天花板只有 7%），而真正的大头都在**感知**和**管道**
   （利用率 40%→99% 带来产出 ×3.4）。
3. **用户比代码更懂这个游戏。** 他给的领域知识（军力=库存、资源=舰艇残骸、
   周内只降不升）每一条都改变了模型的性质，而没有一条是能从代码里推出来的。
   **遇到说不通的现象，先问他。**
