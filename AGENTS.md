# AGENTS.md —— 多个 AI 助手共用的工作记忆

**这份文件是给「下一个接手的 AI」看的。** 它假设你**没有任何会话上下文**：
你没参与过之前的讨论，不知道哪些结论是实测的、哪些是猜的，也不知道哪些坑已经踩过。

登记 2026-08-21。用户口径：

> 因为周额度用完了，我会启动其他 AI agent 来处理。我需要你准备一个 md 文件来保存
> 你的记忆，可能会有多个不同的 AI agent 来共同协作以及维护这个文件。你来规定好规则。

---

## 〇、先读这三样，顺序不能换

1. **[CLAUDE.md](CLAUDE.md)** —— 项目的硬规矩（安全不变量、测试库口径、日志要求）。
   ⚠️ **那份是权威，本文件不复制它的内容**，复制就会变成第二份真相、然后漂移。
2. **本文件第一节（维护规则）** —— 你写东西之前必须先知道怎么写。
3. **本文件第三节（当前状态）** —— 现在有什么在飞、什么卡着。

⚠️ **本文件不是需求文档，也不是设计文档。** 那些在 `docs/` 下，本文件只做索引和状态。

---

## 一、⚠️ 维护规则（用户点名要求的部分）

### 1.1 写「事实」必须带三样：**数值、样本量、日期**

```
✅  稀有 ≈ 0.141 × 军力（实测 n=74，2026-08-18~19，R²=0.781）
❌  军力和收益正相关
```

没有样本量的数字，下一个人无法判断它值不值得信，只能重新做一遍。

### 1.2 ⚠️ 每条结论必须标**证据强度**，四选一

| 标签 | 含义 |
|---|---|
| **实测** | 从生产库或实机量出来的，附样本量 |
| **推断** | 从实测推的，中间有一步没验证 |
| **用户口径** | 用户说的领域知识。⚠️ **不要去验证它，也不要质疑它**——用户比代码更懂这个游戏 |
| **直觉** | 没有数据。⚠️ **必须标出来**，否则下一个人会当事实用 |

**这条是踩出来的**：`docs/周内相位-策略模型.md` 整份是「直觉」，标注清楚之后才没被当成实测。

### 1.3 ⚠️ 推翻旧结论时，**改原处 + 记在第五节**，绝不留两份矛盾的

2026-08-20 就犯过：在《军力被打败会崩-实测》里收回了「指数形式错了」这个说法，
却忘了改 `docs/AI选靶-方案.md` 2.7 表里的同一句，于是两份文档互相打架。
**而那份方案是准备喂给 LLM 的——自相矛盾比说错更糟。**

**做法**：搜一遍这个结论出现在哪几个文件（`grep`），全部改掉，再在第五节写一行。

### 1.4 ⚠️ 拟合出来的系数，**取值范围必须和使用范围对齐**

**这条是最贵的一次教训**：`exp(−0.068×龄)` 这个衰减系数在 0–22 小时上拟得很好
（R²=0.781），但系统只在 **0–6 小时**窗口里工作，而定出那个系数的 **5 个样本全在
窗口之外**。窗口内单独重拟，最优衰减是 **0**，硬套 −0.068 比取平均还差。

⇒ **报一个拟合结果时，必须说清它在哪个区间上拟的、将被用在哪个区间。**

### 1.5 「不知道」要写出来，不许留白

每次交接必须回答三个问题（第六节的模板）：

- 做了什么
- **什么没做完 / 卡在哪**
- **⚠️ 什么是你没验证的**

⚠️ **第三条最重要。** 上一个人没验证的东西，下一个人会默认它验证过。

### 1.6 冲突以什么为准

```
生产库里的数据  >  用户口径  >  代码  >  本文件  >  docs/
```

⚠️ **本文件可能过期。** 发现它和库里的数据对不上，**以库为准，并顺手改掉本文件**。

### 1.7 并发：多个 agent 同时改这份文件

- **第六节的交接日志只 append，写在最上面（倒序）**，冲突面最小；
- **每条署名 + 日期**（`2026-08-21 / Claude` 这种，用你自己的标识）；
- **改第三节（当前状态）之前先重读一遍**，别覆盖别人刚写的；
- **不许删别人写的条目**。要推翻就标 `~~已推翻~~` 并写清理由和日期。

### 1.8 篇幅上限：**500 行**

超了就把**已经落地、不再需要每次读**的内容移进 `docs/` 下的专门文档，
本文件只留一行索引。⚠️ **一份没人读得完的记忆等于没有记忆。**

---

## 二、这个项目在做什么（30 秒版）

浏览器游戏 **Eternal Void** 的**完全外部**自动化：截屏 → 模板匹配/OCR → 真实鼠标点击。
不碰 DOM、不碰协议。两条独立流水线 + 一个 Web 控制台。

**当前的核心目标**：用有限的**航线**（同时在飞的舰队数，现在 9 条）去打 bot 星球，
最大化**核心三资源**（合金碎片/泰坦立方/收割者碎片）的产出。

**唯一的目标函数**：**每航线小时的稀有三样产出**。航线是瓶颈（实测利用率一度 99.2%），
所以「一发能拿多少」不是判据，「一条航线一小时能换多少」才是。

---

## 三、当前状态（2026-08-21 07:30 UTC+8）

### 3.1 代码与部署

```
main            6a36448
迁移 head       a3c81f5d2b64   （单 head）
生产库          a3c81f5d2b64   ← 已同步
开着的 PR       0
```

⚠️ **生产跑在另一台机器**：`CY-202305011401`，`E:\Kucleer_code\EVO\EVO-Helper`。
控制台启动时会往 `system_log` 写一行代码版本（commit / branch / dirty / 迁移前后 revision）。
**要判断生产跑的是哪个版本，查那一行**，不要从「PR 合了」去推。
⚠️ 最近一次记录 `dirty: true` —— 那台机器在 `main` 之上还有**未提交的本地改动**，
内容不可见。有查不通的现象先看这里。

### 3.2 出发点配置（2026-08-20 深夜刚转场）

| 任务 | 出发点 | 航线 |
|---|---|---|
| task#2 一号攻击 | `5:261:8` | 4 |
| task#6 二号攻击 | `8:117:6` | 3 |
| task#8 三号攻击 | `7:228:15` | 2 |

⚠️ **task#2 的扫描范围参数可能还配着老的 `galaxy 4 / 227–327`**，转场后需核对。

### 3.3 在飞的工作

- **保护期邮件解析**（agent 进行中）：认「到达才发现在保护期、舰队返航」那种邮件。
  详见第四节 4.5。

### 3.4 挂着没做的

| 事项 | 卡在哪 |
|---|---|
| AI 选靶一期（影子模式）已上线但**开关默认关** | 两个已知缺陷未修：`AI_MAX_TOKENS=2000` 对推理模型不够（拿到空串）、缺 `reasoning_effort`。用户要先手动调 prompt |
| 衰减项进排序 | **刻意不做**，理由见 `docs/选靶数据跟踪-待办.md` 第八节 |
| 军力历史表 | 用户 2026-08-20 判断**不必做**（攻击日志已够拟收益公式） |
| `_beat_uptime()` 落库失败不限流 | 持续失败会每秒写一条日志（一天 86,400 行）。已发现未修 |

---

## 四、已确认的事实（附证据强度）

### 4.1 【用户口径】核心三资源 = 打爆的舰艇残骸

⇒ `稀有 ≈ 0.141 × 军力` **不是相关性而是恒等式**，0.141 是转换率。
⇒ **军力读数的语义是「剩余可收获库存」**，不是「有多难打」。
⇒ 排序键 `军力 ÷ 往返小时` 字面上就是「库存 ÷ 取货时间」。

### 4.2 【用户口径 + 实测】军力周内只降不升，周一 UTC+0 刷新

被打败会当场掉 **20–93%**（实测 42 条：27 不变 / 14 降），本周期内不再恢复。
⇒ **读数永远是真实军力的上界**。
⚠️ 旧文档里「一周内基线不动」是**错的**，已更正。

### 4.3 【实测】转场到近距离出发点，每航线小时翻倍

```
转场前（4:277:15 + 9:250:8，往返 119/111 分）   1,009 /线小时
转场后（5:261:8 + 8:117:6，往返  49/60 分）    2,045 /线小时    +103%
```

⚠️ **收益里有两部分，性质不同**：往返时长是**位置的永久属性**，打不掉；
「这块地还没被打过」是**临时**的，几天就没。

### 4.4 【实测 → 因果链】本地目标为什么会枯竭

```
高强度攻击 → 打败后军力掉 20–93% → 掉出全服榜前列
→ 军力榜按军力降序只采前 N，再也读不到 → 被 score_max_age_hours 窗口筛掉
→ 候选池只剩跨银河 → 往返变长 → 每航线小时下降
```

⚠️ **这是自我收紧的反馈环，调大 `top_n` 治不了本。** 能打破它的只有
**换出发点**或**等周一刷新**。

### 4.5 【实测】两种保护期，代价差两个数量级

| | 触发点 | 代价 |
|---|---|---|
| 派遣时弹「没有可执行的任务」 | 舰队没起飞 | ~38 秒鼠标时间 |
| **到达才发现（邮件「处于保护状态，我方舰队已返航」）** | 飞完全程 | **一整趟往返的航线** |

⚠️ 近 5 天 **84 发派遣从来没有战报 = 21.1%**，合计白占 **122.8 航线小时**。
**只确认了其中 2 发是保护期**，其余可能是 OCR 失败或还没翻到——**不要把 84 发全算成保护期**。

### 4.6 【实测】基础三样（金属/晶体/气体）由**我方货舱容量**决定，与目标无关

AAA 预设那 6 条的变异系数是 **0.0001**，军力跨 10,020–11,170 纹丝不动。
⚠️ **绝不许进任何「军力→收益」模型**，放进去会让「预设大的星球」无脑领先。

### 4.7 游戏内显示的时刻**就是 UTC**

已用已入库战报逐秒核对（邮件 `20/08/2026 19:22:08` ↔ 库里 `19:22:08 UTC`）。
控制台页面上标的「游戏时间 UTC+0 · 现实时间 UTC+8」是对的。

---

## 五、⚠️ 踩过的坑（每一条都出过事，别重蹈）

### 5.1 工具与流程

- **`SET default_transaction_read_only` 不生效**——它只对**之后开始的**事务管用，
  而 SQLAlchemy 的 Session 在执行它时事务已经开了。
  ⇒ **用 `SET TRANSACTION READ ONLY`**（对当前事务立即生效，必须是本事务第一条语句），
  并**实测一次写被拒**。
- **不许 `git stash`**：`.git` 在多个 worktree 间共享，曾有 agent 弹出了别人的 stash，
  造成 20+ 冲突。
- **不许起 preview / dev server，不许碰主工作区的 `.claude/launch.json`**：
  曾有 agent 因此**误起真控制台、把用户正在跑的那个顶掉**。
- **仓库是公开的**：提交前 `git ls-files | grep -iE '\.(png|jpe?g|webp)$'` 必须为空；
  ⚠️ **用例夹具里不许出现真实坐标**。
- **绝不对任何库跑 `alembic upgrade`**：迁移合进 `main`、由用户重启 bat 让生产自己升。
  **生产库的版本永远不该跑在 `main` 前面**（曾因此让 `main` 的代码启动时
  `Can't locate revision`，跑调度那台机器直接起不来）。

### 5.2 分析与判断

- **⚠️ 用有缺陷的搜索得出「东西不存在」的结论** —— 犯过两次。一次 `grep` 写错说
  「main 有两个 alembic head」，一次 `LIKE '%commit%'` 搜不到启动日志就断言「功能没写」，
  **而那条日志一直在**（消息里根本没有 "commit" 这个词）。
  ⇒ **搜不到时，先怀疑自己的搜索式。**
- **⚠️ 没验证就下结论** —— 「回收率 38% 所以少算」「跨 UTC 日所以对不上」，两个都是
  编出来的解释，查完全错。⇒ **先查再说。**
- **⚠️ 指标的分档太粗会藏掉主因** —— 用「≤90 分内的 Σ军力」给出发点选址，把
  `7:228:15` 排到倒数，而它实测是第一名的 3.9 倍。因为价值是 `军力 ÷ 往返`，
  而 42 分和 84 分被算成了同一档。
- **⚠️ 模型给的是瞬时上界，不是日均** —— 预测转场 +156%，实测 +103%。
  模型只取速率最高的前 10 个 (球, 目标) 对，那是**此刻最好**，不是**一天能持续跑出来的**。
  对没动过的 `7:228:15` 也高估 31%。⇒ **报预测时说清它是上界。**

### 5.3 用户明确否掉的做法

- **不考虑单位数量**（我方与敌方都不考虑）。
- **不要「打过所以打折」** —— 军力读数已经包含了这个信息，再修正是双重计数。
- **盲拖余量的告警不要再提** —— 用户配的没问题。
- **不要动用户的控制台配置** —— 优先级/范围是用户的，发现不符只说不改。
- **工作时间不许起游戏**（工作日 UTC+8 工作时间），实机需当次授权。

---

## 六、交接日志（**只在最上面追加，不要改动已有条目**）

### 2026-08-21 / Claude

**做了**：手工补录 08-20 的 2 份战报（`8:15:13` / `7:204:16`）+ 2 条保护期记录
（`8:485:17` / `9:189:11`）——用户当次授权写生产库。槽位映射用已入库的 `8:69:19`
逐格校准过（不是猜的）。补录后重算转场效果：**+103%**（此前测 +66%，差在战报没回全）。

**没做完**：08-20 还缺 3 发战报（`8:431:20` / `8:223:19` / `8:70:11`），
用户信箱里若还有邮件可以继续补。

**⚠️ 没验证的**：
- 那 84 发无战报的派遣里，只确认 2 发是保护期，其余成因不明；
- 5/8 系被打一整天之后的衰减速度——**这是判断「这次转场能撑几天」的关键**，
  今天（08-21）跑满一天之后才有第一个数据点；
- 生产那台机器 `dirty: true` 的本地改动内容。

---

## 七、给下一个 agent 的三条建议

1. **先看 `docs/选靶数据跟踪-待办.md`** —— 那是一张「等数据」的清单，
   每条写明了今天的结论、靠多少样本、要到多少才够下决定。**别重新做已经做过的分析。**
2. **改动前先量一次现状。** 这个项目里几乎每个「显而易见」的改进，量完之后都比预期小
   （排序层的天花板只有 7%），而真正的大头都在**感知**和**管道**
   （利用率 40%→99% 带来产出 ×3.4）。
3. **用户比代码更懂这个游戏。** 他给的领域知识（军力=库存、资源=舰艇残骸、
   周内只降不升）每一条都改变了模型的性质，而没有一条是能从代码里推出来的。
   **遇到说不通的现象，先问他。**
