---
issue: 317
agent: vision-game
type: Fixed
date: 2026-09-11
---

**⚠️ 残骸框标题不二值化就读成 `'UF A'`，回收在这一步静默失败。**

`#314` 的取证图拍到：**框好好地开着**（标题「回收残骸」清楚、三格资源、绿✓红✗ 都在），
读字 ROI 也框得正正好好。拿那张原分辨率裁片逐个跑配方：

    upscale=3（_read 默认）  → 'UF A'      ← 日志里那串，精确复现
    upscale=4                → 'UF'
    upscale=5 / 6            → 'DUI' / 'EIU'
    upscale=4, threshold=120 → '回收残徽'  ✅

⇒ 真因是没二值化（浅色字压在蓝色渐变上），同 `make_ocr` 里 START 那条。
**光加 upscale 越放越糟**，所以「多重试 / 放大点」两条路都是死的。

⚠️ 二值化后仍会错一个字（「骸」→「徽」），靠 `#311` 的模糊匹配兜住，用例钉死。

## ⚠️ 同时更正 `#315` 的因果判断

`#315` 量到的「点击 y=420 落在图标(362…416)与标签(429…440)之间的空隙」是事实、
也该修（同一排按钮不该用两个 y，这个错误本仓犯过三次），
**但它不是那些失败的原因** —— 框一直是开着的。
`#315` 之后 44%→14% 的改善多半是偶然，不记在它头上。

- Database: 无迁移
- Verification: `pytest -q`（4464 passed / 256 skipped）/ ruff / mypy 全绿；配方在实拍像素上验过
- Safety: 只改这一处读屏配方
- Rollback: revert 本 PR
