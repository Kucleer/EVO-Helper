---
issue: 344
agent: vision-game
type: Added
date: 2026-09-14
---

**二级标签判据对不上时，把每行主题原文打进日志。** 纯取证，不碰判据。

## 为什么

2026-09-14 晚实机：这一档连着 **10 次**都是「舰队类 N 行、非舰队类 1 行」，
于是切不到「舰队」、整趟放弃、**回收读信每趟 0 份**（库里 325 发在等实收）。

而光看计数**分不出**两种相反的病因：

    A. 筛选确实开着，只是有一行读花成攻击报告  ⇒ 判据太严，该放宽
    B. 筛选根本没切过去，列表里真混着攻击报告  ⇒ 判据是对的，该查点击

⚠️ 两者处置完全相反。而 09-13 夜那次事故的剧本正是
「判据过不去 → 放宽它 → 真正的毛病被盖住」，所以**分清之前不动判据**。

## ⚠️ 为什么不是「加存图」

那一档**本来就存图**（`mail-sub-tab-…-unconfirmed`），当晚也确实存了。
但**生产跑在 `CY-202305011401`，排障在另一台机器** —— 图够不到。
日志进的是共享库，两台都读得到。

**判据的输入必须进日志，不能只进图。**
（这一条比这次的具体问题更通用：凡是「排障者与被排障的机器不同台」的场合，
落盘的证据都可能等于没有。）

## 输出长这样

    二级标签「舰队」内容对不上（舰队类 1 行、非舰队类 1 行、共 6 行）
      └ 第 0 行 kind=FLEET_RETURN 主题='eS  舰队返回'
      └ 第 1 行 kind=ATTACK       主题='AN SO 攻击报告 iva'
      └ 第 2 行 kind=UNKNOWN      主题='~~ — —w————'

⚠️ 「点之前先看一眼」那一次（`quiet=True`）**不打** —— 它不匹配是正常的，
打了会把日志刷满。用例反向钉住了这一条。

- Configuration: 无
- Database: 无
- Verification: `ruff` 全绿；`pytest` 4650 passed / 0 failed。
- Safety: **纯新增日志**，不碰任何判据与控制流。唯一的代价是失败时多几行日志，
  而那一档本来就不常见（常见的话正说明有问题要查）。
- Rollback: 回退即可；回退后只是少了这几行诊断。
