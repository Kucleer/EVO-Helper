---
issue: 18
agent: root
type: Changed
date: 2026-08-07
---

把剩余页面并入控制台外壳，主导航收敛到「任务中心 + 情报中心」。

- `runs.html` / `run.html` / `diagnostics.html` 改用深色控制台外壳，中文化，表单压成紧凑单行。
- 运行状态徽标共用 `run_state_tone` / `run_state_glyph`：**每个状态同时给出颜色、字形和状态名**，
  颜色从不是唯一信号。未知状态回落到 `•` 而不是报错。
- 运行详情页提供暂停 / 恢复 / 紧急停止，并写明急停优先于任何状态机动作、鼠标甩到左上角可立即中断。

旧页面不再留死链：

| 旧路径 | 去向 | 原因 |
|---|---|---|
| `/` | `/missions` | 仪表盘已并入任务中心 |
| `/plans` | `/missions` | 计划配置已并入任务中心 |
| `/targets` | `/intel` | bot 列表升级为情报中心，且支持按舰队筛选 |

三个模板 `index.html`、`plans.html`、`targets.html` 随之删除。`/plans` 一度同时存在
重定向和旧路由两份注册，已移除旧的那份。

- 配置：无变更
- 数据库：无变更
- 验证：`pytest`（312 passed）、`ruff check src tests`、`ruff format --check src tests`、`mypy src`；
  新增用例覆盖三条重定向、辅助页渲染在控制台外壳内、状态徽标颜色必配字形；
  浏览器实测七条路由全部 200，并跑通「启动运行 → 暂停 → 恢复 → 紧急停止」与强制复查，
  状态事件时间线逐条正确
- 安全：只是页面整合，未新增任何派遣路径；`dry_run` 仍为 true
- 回滚：恢复三个模板与其路由，撤销重定向
