---
issue: 347
agent: vision-game
type: Fixed
date: 2026-09-15
---

**切回战报要点「战斗」** —— 取消舰队之后列表是空的，不是显示全部。
⚠️ 这一条**推翻我自己 `#336` 的结论**；攻击战报整晚读不到，真因就在这里
（09-14 06:37 → 09-15 02:00，一份都没有）。

## 实拍证据

`dump-mail-list-empty-014934.png`（2026-09-15 01:49，生产本机）：

    战斗 [11]   侦察   舰队 [99+]   系统      ← 第二排一个筛选都没选中
               列表正中：「没有符合当前筛选条件的邮件。」

**取消「舰队」之后不是「显示全部」，是什么都不显示。**
角标明明写着战斗 11 封、舰队 99+ 封，而列表是空的。

## `#336` 错在哪

`#336` 判定「那一排只有『舰队』有反应，它是个筛选开关」，于是把「切回去」
实现成**再点一次舰队把它取消**。

而 `#336` **之前**的代码本来就是点「战斗」切回去的 —— 只是坐标抄错
（897 是**侦察**的 x）。改对成 772 之后那一次没生效，于是我把整条路否掉。
**方向是对的，被我当成错的删掉了。**

⚠️ 这也是「一次没生效」与「这条路不通」之间的区别 ——
当时我拿前者下了后者的结论。

## 改

| 方向 | 点哪个 | 判据 |
|---|---|---|
| 要舰队 | `MAIL_FLEET_SUB_TAB = (1033, 218)` | `here > 0 and other == 0`（照旧） |
| **要战报** | **`MAIL_BATTLE_SUB_TAB = (772, 218)`** | 列表**不是空的**（空列表那道闸 `#346` 排在前面并 return） |

⚠️ 「切到战斗」那一侧**不要求正面看见攻击报告**：那些主题读不准
（实测 87% 读不出），要正面证据等于永远判不成立 ——
那正是 2026-09-14 中午「每轮自杀」的成因。

⚠️ 标签名同步改成「战斗」，否则日志会骗人。只读安全闸已核：不沾 `FORBIDDEN_LABELS`。

## 用例

原先 `test_both_directions_click_the_same_button` 钉的正是 `#336` 的**错误结论**，
已重写：两个坐标必须不同；各自的 x 必须落在实拍量出的区间内
（战斗 713~830、舰队 965~1082）—— **以后再抄错坐标会当场红**。

- Configuration: 无
- Database: 无
- Verification: `ruff` 全绿；`mypy` 3 个既有错；`pytest` 4656 passed / 0 failed。
  ⚠️ **实机验收未做** —— 要看重启后「攻击战报入库 > 0」。
- Safety: 只改切标签点哪个按钮。最坏情况是「战斗」那一下不生效，
  那时列表仍是空的，而 `#346` 的空列表闸会让这一趟直接收手（不开幻影邮件）。
- Rollback: 回退即可；回退后回到「取消舰队 ⇒ 列表空 ⇒ 攻击战报读不到」。
