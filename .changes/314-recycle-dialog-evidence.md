---
issue: 314
agent: vision-game
type: Added
date: 2026-09-11
---

**残骸框没弹出来时留原分辨率取证。**

实机 04:43 `5:263:12`：回收按钮**找到了也点了**，标题却读到 `'UF HK'` ——
拉丁乱码，模糊匹配再放宽也够不着。这条支线只记字符串，
分不出「框还没画完 / ROI 落偏 / 点开了别的窗口」，而三者的善后完全相反。
这一夜 `screen_error` 已 6 次（对 `dispatched` 7 次）。

⚠️ **不动任何判据，只加取证** —— 同「没有回收按钮」在 `#309` 之前的处境，
那次连猜三个原因全错，是取证图否掉的。

`RECYCLE_DIALOG_EVIDENCE_ROI = (700,300,1250,640)`，**故意比读字的
`(900,350,1020,380)` 大一圈**，下边界够到 583 那行按钮
（「框在不在」比字形可信）。限流 120s，取不到帧不抛。
调用点排在 `_reset_to_known_screen()` **之前**，用例变异验证过。

- Database: 无迁移
- Verification: `pytest -q` 全绿；顺序断言变异验证会红
- Safety: 纯观测，不改任何判据
- Rollback: revert 本 PR
