---
issue: 340
agent: vision-game
type: Fixed
date: 2026-09-14
---

**全线停摆 57 分钟** —— `#339` 加的那道前提检查，在主题读不出时**必然**判死这一轮。

## 事故

`#339` 上生产后每一轮都死在开工那一步：

    这一轮判为失败：开工翻不了信箱：重开之后仍然翻不了
    （舰队筛选关不掉，拒绝在筛过的列表上翻战报）；单子上 3 发到点没战报

任务 2/6/8/9/10/11/12 一起挂、被判成环境故障。
**最后一发派遣停在 08:24，扫描任务也起不来。**

## ⚠️⚠️⚠️ 同一个不对称，我栽了两次，方向相反

实测事实只有一条：**舰队类（舰队返回 / 回收报告）主题读得准（6/6）；
非舰队类（攻击报告…）读不准，整屏 unknown 是常态。**

| | 我怎么写的 | 代价 |
|---|---|---|
| 09-13 夜 | 「关掉」那侧放松成「没看见舰队类就算数」 | 筛选卡住没人发现，攻击战报断流 **9 小时**、37 发无战报 |
| 09-14 午 | 改严成「必须正面看见非舰队类」 | 那恰恰是读不出的那一类 → 永远确认不了 → **每轮自杀** |

生产日志把破绽写死了：

    二级标签「舰队筛选关」内容对不上（舰队类 0 行、非舰队类 0 行、共 6 行）

舰队类 0 行 ⇒ **筛选根本没开着**。而判据要的正面证据落在读不出的那一类上。

## 修

**① 只在「筛选开着」这一个方向上判** —— 唯一拿得到正面证据的方向。
「关掉了」不去正面确认，它等价于「认不出开着」（`_fleet_filter_looks_on()`）。

**② 放弃这一趟的条件 = 「正面认出开着、而且关不掉」**，不是「确认不了它关着」。
⚠️⚠️ **「查不出来」不许判死这一轮。**

**③ 「开着」的判据改成整屏全中**（`here == len(kinds)`）。
原先的 `here > 0 and other == 0` 在**没筛过的列表上也会成立** ——
舰队类占信箱三分之二，那一屏碰巧全是舰队类、攻击报告又读不出（常态），
那时代码会以为筛选开着、点一下，**反而把筛选打开**，退回赌奇偶。

## ⚠️⚠️ 发布前复核推翻了我自己三处

用户要求发布前再自查一轮。三处**都是在我说「验过了」之后**才查出来的：

**① 阈值凭印象定，会漏掉一半。** 我把「筛选开着」收紧成「整屏全中」，依据只是
昨晚几次观察。把库里**全部 216 次**记录拉出来数：

    筛选开着（28 次）： 6/6 行 46% · 5/6 行 42% · 4/6 行 10%   ← 永远 ≥ 4
    筛选关着（188 次）：0/6 行 **100%**

「整屏全中」**会漏掉 54% 的卡住筛选**。两侧之间本来就有大空档，
正确阈值是 `here > 0`。这组数已写死在判据的注释里。

**② 那道前提检查放在信箱打开之前，是个空操作。** 信箱是在 `_scan_mail_rows` 的
`_enter_mailbox()` 才开的，而我把检查写在 `_scan_for_reconcile` 开头 ——
探测读的是恒星系视图，一行都读不到，**每轮白花一次 OCR、零保护**。

**③ 收手方式仍是抛异常。** 位置挪对之后，「关不掉就抛」这个形状还在 ——
而那正是停摆 57 分钟的成因。改成走 `_scan_mail_rows` 里现成的 `cut_short`。

**④ 守卫全在扫源码文本，行为一个字没验**（同 `tests-can-guard-a-bug`）。
判据行写得漂亮守卫就绿，而这次正是判据行写得漂亮、生产上每轮自杀。
真值表已写成正式用例。

## 验

真值表 6 种屏幕全对，含**生产当时正在自杀的那一屏**（6 行全 unknown → 放行）。
全量 4642 passed / 0 失败。

⚠️ 那 39 个用例失败也是我的：`_fleet_filter_looks_on()` 自己要读一屏，
把夹具 pop 的那一屏吃掉了 —— 正是 `#339` 提交信息里我自己写过的警告。
改成桩那个**唯一入口**（`_fleet_filter_looks_on = lambda: False`）。

- Configuration: 无
- Database: 无
- Verification: 真值表（含生产现场那一屏）；全量 4642 passed。
  ⚠️ **实机验收还没做** —— 要看的是「每一轮不再判失败、派遣恢复」。
- Safety: 这一版比事故前**更不容易判死轮次**（放弃需要正面证据），
  同时比 `#339` 之前**更能发现卡住的筛选**（整屏全中那一条）。
- Rollback: 回退到 `a57d3c5`（`#338` 合并点）即可，那一版没有这道前提检查；
  ⚠️ 它带着 `#339` 修的那个静默饿死 bug，但那个 bug 需要「回收读信开着」才触发。
